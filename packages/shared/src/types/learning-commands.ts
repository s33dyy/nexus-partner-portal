export type SubmitAssessmentInput = {
  assessmentId: string;
  score: number;
};

export type EnrollInTrackInput = {
  trackId: string;
};

export type CompleteLessonInput = {
  lessonId: string;
};
