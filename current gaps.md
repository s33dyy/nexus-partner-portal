# Current Gaps — `product.md` vs. the shipped product

| Attribute | Value |
| --- | --- |
| Analysed | 31 July 2026 — full re-audit |
| Specification | `product.md` (Blueprint v1.0, 6,091 lines, 23 chapters) |
| Codebase | `main` @ `653c44d`, 32 routes / ~20k route LOC, `db/schema.sql` (67 tables) |
| Method | Fresh chapter-by-chapter re-audit: each of the 17 substantive chapters (4–19, 23) independently re-read in full against `product.md`, then verified directly against the current routes, server commands, domain contracts, and schema by a dedicated agent — not diffed against the previous version of this document |
| Related | `docs/implementation-status.md` (this session's fix log — every item below already fixed and deployed is cross-referenced there) |
| Addendum | §21 below — a 2026-08-05 independent full-backend re-audit (12 domain agents, spec-vs-code) confirms essentially every open item below is still open, and surfaces one new item: an undocumented Call Center/Twilio Voice backend added after this pass (commit `81f48a4`) |

This document **replaces** the previous `current gaps.md` in place. It is a fresh pass, not a
diff — every chapter was re-read against the current code from scratch, so an item's absence
below means this pass found it correct now, not that it was skipped. Each agent was briefed on
everything already fixed and deployed this session (listed below) so it would not re-report
closed items, but was otherwise told to verify, not assume — several new S1 items surfaced
that the original audit's narrower first pass had not caught, alongside confirmation that all
of the previously-fixed items remain fixed.

> **Fixed and deployed this session, before this re-audit ran (verified still correct below):**
> all 9 canonical roles resolve at runtime and can log in; the Negotiation → Won pipeline
> transition works; rewards now award on outcome-review PO approval (not on `stage = won`) and
> prefer the frozen Pricing Revision's DTP total; `support_ticket_comments.is_internal` exists
> and is enforced server-side, with LIVEY-internal roles able to read tickets they didn't
> personally create; Settings' Governance/Configuration sections are gated to super_admin only;
> `portal_audit_events.actor_id` is stamped server-side, not client-supplied; document
> upload/download/delete now checks real authorization (previously had none); `document_blobs`
> and `password_reset_tokens` are denied outright via `SERVER_ONLY_TABLES`;
> `createWorkspaceUser`/`createWorkspaceUsersBulk` are super_admin-only with a real
> allowed-roles check; `createPartnerTeamMembersBulk` validates the caller's own company;
> `uploadCloudinaryImage` requires super_admin; `changeUserRole` validates the target's own
> tenant, not just the grantable role; Region access moved from role-level to per-user
> configuration; `deal-documents` review modal no longer auto-opens/fails to close;
> `dropdown-sources.server.ts` (previously had **zero** authentication, exposing every
> partner/customer/profile system-wide) now scopes every caller to their own tenant; the
> duplicate `products`/`price_books` table definitions were confirmed already resolved;
> `deal-line-items.tsx`/`deal-activity-timeline.tsx`/`deal-participant-tags.tsx` read/write real
> data instead of fabricated stubs; the dashboard and Insight Hub are no longer fully broken;
> `approveDiscount` now checks deal/partner/geography scope, not just role.

Severity legend:

- **S1 — Blocking / unsafe.** Violates a security, financial, or acceptance-criteria
  invariant, or the feature is functionally broken in the live app.
- **S2 — Major.** A required capability is absent or is present only as a non-functional stub.
- **S3 — Divergent.** The capability exists but does not match the canonical model, labels,
  or contract.

---

## 1. Summary by chapter

| Ch. | Area | State | Worst severity |
| --- | --- | --- | --- |
| 4 | Experience and interaction system | Partial — header omits breadcrumb/help/true context switcher, search boxes remain in 2 pages, saved views/column controls and unsaved-changes prompts are entirely unbuilt, region/state fields allow ad hoc creation, assistant doesn't drive structured filters, and Insight Hub certificates are a toast stub | S2 |
| 5 | Hierarchy, identity, context, RBAC | Partial/broken — the governed Assignment/ActiveContext contract (governance.ts) is well-designed but barely wired up: 2 of the 3 user-creation paths never grant an `assignments` row at all (breaking feature-gated access for those accounts entirely), internal-role deal visibility collapses to self-created-only on the primary Deals/Pipeline pages, and §5.7 automatic tagging/Coverage Exception, the §5.2 status lifecycle, §5.3 Active-Context switching, and the §5.4 Distributor command allowlist are all unbuilt beyond data-model scaffolding. | **S1** |
| 6 | Authentication, onboarding, profiles, teams | Partial — core sign-in/session/document-access plumbing is sound, but registration has no real email-verification gate and password-reset leaks the raw token to whoever requests it (account takeover); the partner lifecycle is missing Suspended/Offboarded entirely and its own state machine is never invoked by the real write path; admin/partner-invite flows let the inviter set a user's plaintext password with no forced reset; team invitations and the onboarding wizard omit numerous required §6.4/§6.9 fields; Changes Requested reopens the whole form; and the review workspace lacks duplicate-detection, SLA cards, and internal/external note separation. | **S1** |
| 7 | Navigation and role dashboards | Nav shell + generic dashboard render for everyone, but §7.3-7.9's per-role dashboards, §7.2's common-anatomy panels, and §7.1's Distributor exclusions are all unenforced/unbuilt at the UI layer despite a correctly-modeled role/flag layer underneath. | S2 |
| 8 | Partners, Customers, Contacts | Core objects (Partner, Customer, participant tags, merge governance) exist and mostly wire to real Postgres data, but the Distributor-tag security model in §8.7 is inverted (grants whole-tenant access instead of tag-gated access), the named Distributor commands and the Contact/POC entity from §8.4 were never built, and the §8.5 next-step workflow and §8.1/8.2 partner admin surface are stubs. | **S1** |
| 9 | Deals, pricing, approvals, pipeline | Registration status is architecturally collapsed into pipeline_stage in 3 independent code paths (violating the ch.9 core invariant); Won/Lost close bypasses the PO/outcome-review flow entirely and can permanently orphan a deal from ever reaching Approved Won/rewards; the PO-review action panel is shown to PAM/RM/KAM/ISR but the server accepts only literal super_admin; pipeline "pipeline value" wrongly sums Won+Lost off a free-text amount instead of stage-scoped DTP. Backward movement, per-line product/pricing capture, participant auto-tagging, and the discount-approval workflow are all fully built server-side but have zero UI entry points anywhere in the shipped product. | **S1** |
| 10 | Tasks | Partial/broken — the 5-state transition machine itself is modeled correctly, but a generic-table write path lets any role bypass it entirely, "My Tasks" is invisible to assignees for every internal role, and §10.1 data model, §10.4 pipeline integration, §10.3 workspace views, and §10.6 reminders/escalation are essentially unbuilt. | **S1** |
| 11 | News, Activity, Notifications | Activity model (domain_activity_events) is genuinely implemented and append-only; but Notification recipient targeting is broken (self-notifies + partner-wide broadcast, no ticket/task notifications at all), News (portal_news_posts) is a bare CRUD photo-post feature missing the entire lifecycle/audience/digest/ack/revision/analytics model, deal activity gets fabricated into the globally-public News table by super_admin actions (cross-tenant leak), and there is no Delivery/digest subsystem (no email/WhatsApp transport, no delivery-state table, no preferences) anywhere in the codebase. | **S1** |
| 12 | Assistant, website/WhatsApp, Auto CRM | Partial — in-app Assistant works only for two capabilities (list own Deals, draft+confirm a new Deal) vs. ~15 required by §12.2; Auto CRM lead lifecycle, website business assistant, and WhatsApp workflow (§12.5-§12.11) are entirely unbuilt, with LEAD_STATE_MACHINE an orphaned 5-state stub with no backing table, UI, or caller. | S2 |
| 13 | Support and ticketing | Ticket creation itself crashes on every call (missing DB columns) and livey_support is UI-locked out of all ticket actions; beyond that, the ticket data model/SLA policy/notifications/workspace are minimal stubs covering only a fraction of §13's required fields. | **S1** |
| 14 | Insight Hub | Partial — enrollment and tier-gating work, but the entire lesson/assessment/certification pipeline beyond enrolling is unbuilt (no lesson viewer, no question bank, no real Certificate entity, no per-audience assignment model); admin authoring stops at Track/Subject CRUD with lesson creation hard-disabled. | S2 |
| 15 | Rewards | Reward award trigger/basis fixed this session, but redemption flow has no reservation/stock/atomicity (S1), contributor-eligibility gate for LIVEY/Distributor is unenforced (S1), the §15.2 calc model and §15.9 gadget-catalogue fields are largely absent (S2), and the Reward Store shows a fake points-derived tier disconnected from the governed partner tier (S3). | **S1** |
| 16 | Analytics, imports, exports, settings | Partial/regressed — profiles reads (incl. the §16.2 profiles export) leak password_hash to the client because the server always runs SELECT * regardless of the requested column list; exports are never audited and lack any async/progress path; every import flow commits immediately with no dry-run/confirm/rollback; Settings is still literally an "Export hub" missing nearly all required IA; reward catalog items can be hard-deleted with cascading loss of redemption history; and policy versioning/impact-preview/dual-approval are entirely unbuilt. | **S1** |
| 17 | External integrations | Partial/broken — Zoho Sign is the only real provider client, but its send/resync HTTP routes have zero auth and its webhook signature check fails open; Zoho Books, WhatsApp, Email, and DHL have no server-side adapters at all; the GyFTR client is dead code with zero callers; command_outbox/command_inbox exist in schema but nothing drains or writes to them; the Super Admin Integration Operations Centre is 100% fabricated client-state mock data. | **S1** |
| 18 | Canonical data model | Partial — the governed catalogue/pricing model (products/product_variants/product_skus/combos/price_books/price_rows/fx_snapshots) is schema-only with zero application usage while the live catalog runs on legacy free-text portal_catalog_items; Deal Line Items have no create path anywhere in the app; the Activity Timeline/dashboard activity feed silently renders empty for every non-super_admin viewer; and no domain command ever populates a real idempotency key despite the contract requiring one. | **S1** |
| 19 | Security, privacy, reliability, performance | Auth/session hardening is the weak core: password-reset hands the live reset token straight back to whoever submits the form and reveals account existence (S1), and no code path ever revokes an existing session on password reset, admin-forced reset, or role change despite a flat 14-day token lifetime for every role including super_admin (S1). MFA, rate limiting, email verification, auth/session/recovery audit logging, provider-secret field encryption, CSP/CORS/frame-ancestors headers, retention/deletion + data-subject workflows, backup/observability/alerting, and genuine accessibility test coverage are all effectively unbuilt. Large list views and a bulk policy-check loop are unpaginated/N+1. | **S1** |
| 23 | Governed defaults and reference data | Reference-data governance (§23.1/§23.4) is largely unimplemented as a single canonical registry: taxonomy.ts matches blueprint exactly in only 2 of 17 named dictionaries (Assignment status, Deal pipeline/probability); 6 dictionaries (Coverage Exception, Distributor fulfillment, Certificate status, News, External delivery, Shipment) have zero backing anywhere in the DB or server code; Task/Ticket are correctly implemented in live command modules but via code that explicitly bypasses the wrong "canonical" file, leaving duplicate/conflicting definitions of the same concept. | S2 |
Chapters 20 (migration), 21 (test catalogue), and 22 (traceability) are process
chapters; their gaps are folded into §12 below (Chapter 13 — Support and ticketing), matching
the previous version of this document's own numbering convention.

---

## 2. S1 gaps — fix before anything else

### 2.1 Ch.5 5a (§5.2) — Two of three account-creation paths never grant a governed Assignment — **fixed 2026-08-01**

**Spec (§5.2):** "A user identity... does not directly grant business access. Access is granted through one or more assignments" — every user, whatever their onboarding path, must end up with a governed Assignment to have any business access.

**Shipped:** Two of the three code paths that create a real partner account never insert a row into `assignments`: signUpLocal (src/server/livey-service.server.ts:1504-1525, self-service partner registration — inserts into `profiles`/`user_roles` only) and createPartnerTeamMembersBulk (src/server/livey-service.server.ts:1731-1821, a Partner Admin inviting team members — calls insertWorkspaceUserRecord at :1647-1703, which also only inserts `profiles`/`user_roles`, then a `portal_team_members` row). Only the third path, admin.users.tsx's super_admin-only createUser flow, explicitly follows up with assignGovernedRole (src/routes/_authenticated/admin.users.tsx:376) to create a real assignment. Every generic table read/write goes through queryTable -> applyTablePolicy, which derives `governedRoleKey: authContext.assignment?.roleKey ?? null` (src/server/livey-service.server.ts:1144) and then calls assertGovernedFeatureCapability (src/server/table-policy.server.ts:933-943), which loads capabilities via loadRoleCapabilities(auth.governedRoleKey ?? null) (src/server/rbac-policy.server.ts:38-40) — that function returns `emptyCapabilities()` (all four CRUD flags false) immediately whenever roleKey is null, with no fallback to the `roles` array anywhere in either file (verified by grep across table-policy.server.ts/rbac-policy.server.ts/livey-service.server.ts). The result: any self-registered partner_admin or any partner-admin-invited partner_user/partner_admin permanently has governedRoleKey = null and is denied on every operation against every TABLE_FEATURE_MAP table — portal_deals, partners, portal_customers, tasks, support_tickets, deal_documents, reward_*, portal_audit_events, portal_news_posts, learning_* — i.e. the entire product surface those accounts would use. The demo/seed script (scripts/prod-demo-fixtures.ts:611-780) sidesteps this by hand-inserting `assignments` rows directly for every demo profile, so seeded-data verification would not have surfaced it.

**Fix:** `assignGovernedRole` (the one working path) can't be reused directly for either broken path — it requires an acting `GovernedActor` authorised via `authorizeAssignRole` (super_admin only), which doesn't exist for a brand-new self-registering user and wouldn't authorise an inviting partner_admin either. Added a minimal, purpose-built `bootstrapPartnerAssignment(client, {userId, roleKey, source, approverUserId})` — no acting-on-behalf-of authorization needed, since the assignment is a normal side effect of account creation, not a privileged admin action. Inserts a real, active `assignments` row (geography ceiling defaults to Global — confirmed `role_geography_access` has zero rows for partner_admin/partner_user today, and geography scope narrowing is a LIVEY-internal-role concept, not a partner one) plus the matching `active_contexts` row the UI's own "Active context unavailable" gate requires. Wired into both `signUpLocal` and `createPartnerTeamMembersBulk`.

**Verification:** Unit tests for `bootstrapPartnerAssignment` directly (both partner_admin and partner_user), proved via the same revert/fail/restore cycle as the rest of this session. `signUpLocal` itself couldn't be unit-tested end-to-end (it calls `setCookie`, which needs the same ambient request context TanStack Start's `getAuthContext()` needs — a known constraint this session hit before) — instead verified with a real browser signup against the local dev DB: previously this would have left the account permanently denied everywhere; now the post-signup screen correctly shows "Assignment active" (not the "Active context unavailable" blocking screen my own earlier RM-test-user setup hit before I'd created its assignment/active_context rows by hand), and a direct database query confirmed a real `assignments` row (`role_key=partner_admin`, `status=active`, `source=self_registration`) and matching `active_contexts` row. `createPartnerTeamMembersBulk` uses the identical, already-proven `bootstrapPartnerAssignment` call — not independently re-verified through the full 5-step partner onboarding UI given the time already spent this session, but the shared mechanism is proven both in isolation and end-to-end via the signup path.

---

### 2.2 Ch.5 5b (§5.5/§5.6) — LIVEY-internal roles can only see deals/tasks they personally created, not their authorised scope — **fixed 2026-08-01**

**Spec (§5.5/§5.6):** The permission matrix gives RM "S" (authorised assignment scope), PAM/KAM "Assigned/T", and ISR "T" for View Deal — visibility must be the intersection of active assignment, geography scope, and participant/tag relationship, never collapsed to self-created-only.

**Shipped:** For portal_deals, getScopeSpec (src/server/table-policy.server.ts:361-368) returns `{column: partner_id}` when auth.partnerId is set, else `{column: user_id, value: auth.userId}` — LIVEY-internal roles (rm/pam/kam/isr/livey_support) have no partnerId, so they fall into the user_id=self branch, and this filter is applied unconditionally at :710-765 with no analogous carve-out to the one added for support_tickets (hasGlobalLiveySupportAccess, used only at :718 and :543, table === "support_tickets" only). appendGeographyFilter (:945-963) only ADDS a country-in-ceiling filter on top of this, it does not replace it. The two primary UI pages that list deals — deals.tsx (src/routes/_authenticated/deals.tsx:339-348) and pipeline.tsx (src/routes/_authenticated/pipeline.tsx:83-89) — both also apply the identical client-side restriction via applyPartnerScope (src/lib/partner-scope.ts:16-24: `.eq(fallbackColumn ?? "user_id", input.userId)` whenever partnerId is absent). Net effect: an RM/PAM/KAM/ISR/Support user can only ever see deals they personally created, never deals in their region, assigned Partner/Customer, or explicitly tagged to them — the opposite of the S/Assigned-T/T grants the matrix requires. The code's own comment at :168-178 acknowledges this exact class of bug was fixed only for support_tickets and "likely exists on other ownership-scoped tables... needs its own review per table" — portal_deals (and tasks, same getScopeSpec pattern at :406-414) is that unreviewed table.

**Fix:** Added `isLiveyInternalRole(auth)` (rm/pam/kam/isr/livey_support only, never restricted_distributor, who has a real partnerId and stays tenant-scoped). For `portal_deals` reads, this bypasses the ownership filter entirely and lets the existing `appendGeographyFilter`/`GEOGRAPHY_SCOPED_TABLES` machinery do the real narrowing by the caller's geography ceiling — a deliberate interim step (the spec's fuller PAM/KAM/ISR model is participant-tag-based per §5.7, but that auto-tagging engine doesn't exist yet per §5c/§9g, still open). Also fixed the identical, previously-undiscovered client-side re-narrowing bug: `deals.tsx`/`pipeline.tsx`'s `applyPartnerScope(...)` call was applying the OLD self-only filter on top of an already-correct server response, silently zeroing it back out — added a `bypassOwnershipFilter` flag, computed client-side via a new `hasDealsScopeBypass(roleKey)` helper. While tracing this found the SAME class of client-side re-narrowing bug already present for `support_tickets` (from an earlier session's server-side fix) — `support.tsx` still applied its own stale self-only filter; fixed with an equivalent `hasSupportScopeBypass` helper matching the server's `hasGlobalLiveySupportAccess` (Global-ceiling-only) condition exactly. See `docs/implementation-status.md` for three further bugs discovered and fixed while verifying this end-to-end (a broken client-side assignment-row field mapping, `portal_team_members` throwing "Access denied" for any caller with no company, and `support_ticket_comments.is_internal` missing from the server's column allowlist).

---

### 2.3 Ch.6 6a (§6.1/§6.2/§6.7) — No email verification; password-reset hands the raw token back to whoever asks — **token-leak/enumeration half fixed 2026-08-01, email verification still open**

**Spec (§6.1/§6.2/§6.7):** Registration must send email verification and unverified identities cannot enter onboarding until verified (§6.2 steps 2-4); passwords/reset secrets must never be exposed and must travel through a secure (email) channel, never displayed to the requester (§6.1 'Passwords are never sent, displayed, exported...', §6.7 'through a secure channel').

**Shipped:** signUpLocal (src/server/livey-service.server.ts:1504-1556) creates the profile and a live session in one step with no email-verification concept anywhere in the schema or code; routes/auth.tsx's SignUpForm (lines 250-277) signs the user in and navigates straight to /partner/onboarding. Separately, requestPasswordReset (src/server/livey-service.server.ts:1844-1865) returns the actual reset link containing the plaintext token directly in the RPC response to ANY caller (even unauthenticated) who supplies a target email, and routes/auth.tsx's ForgotForm (lines 357-380) copies that link to the requester's own clipboard and shows it as a success toast — no email is ever sent. Anyone who knows a user's email address can obtain a working password-reset token for that account and take it over.

**Fix (partial, 2026-08-01):** `requestPasswordReset` now always returns `{ ok: true }` regardless of whether the account exists — the token is still generated and stored, but only logged server-side (`console.info`), never returned to the caller. `ForgotForm` shows an identical "If that account exists, a reset link is on its way" message either way. This closes the account-takeover/enumeration primitive (see §2.23, same function) but is a stopgap, not real delivery: since no email transport is configured, self-service reset in production requires an operator with log access to relay the link out-of-band. **Still open:** no email-verification gate on signUpLocal/registration (the other half of this finding), and real email delivery to replace the log-relay stopgap (tracked in Phase 5 of §20's phased plan, which needs email/WhatsApp transport for digests anyway).

---

### 2.4 Ch.8 8.7a (§8.7/§8.8) — Distributor gets whole-tenant access instead of only explicitly tagged records — **Deal/Customer half fixed 2026-08-05, Task/Ticket half still open**

**Spec (§8.7/§8.8):** "Distributor assignment grants no record visibility until a Customer or Deal tag exists"; "A Distributor cannot ... retain access merely because a cached card ... exists"; §8.8 acceptance criterion: "Distributor cannot discover an untagged Customer."

**Shipped:** The command-layer authorization functions that gate every Deal/Task/Ticket mutation treat restricted_distributor identically to partner_admin/partner_user: access is granted to ANY record whose partner_id matches the distributor's own assignment.partnerId, with zero reference to customer_participants/deal_participants tag membership. Identical block in all three: src/server/deal-commands.server.ts:92-105 (authorizeDealActor), src/server/task-commands.server.ts:86-99, src/server/ticket-commands.server.ts:93-106 - `if (role === 'partner_admin' || role === 'partner_user' || role === 'restricted_distributor') { if (partnerId !== record.partner_id) deny; else allow; }`. A full read of table-policy.server.ts (getScopeSpec/getGenericScopeSpec, lines 205-439) confirms no EXISTS/JOIN against customer_participants or deal_participants is ever consulted for any role. Net effect: once a Distributor is assigned to a partner tenant (via admin.users.tsx's per-user assignment flow), they can read/update/advance-stage/delete every Deal, Task, and Ticket belonging to that entire partner - not only ones an RM/PAM explicitly tagged them onto via the Customer/Deal participant commands - directly contradicting the chapter's repeated "no visibility until tagged" invariant. This is a tenant-isolation violation, not a missing feature.

**Fix (Deal/Customer, 2026-08-05):** `authorizeDealActor` (deal-commands.server.ts) and the new `authorizeCustomerActor` (participant-commands.server.ts) now give `restricted_distributor` its own branch, split out from the shared `partner_admin`/`partner_user` one: after the existing partner_id match, an *additional* check requires an active row in `deal_participants`/`customer_participants` (`participant_user_id = actor.userId AND valid_to IS NULL`) before any write is allowed — a brand-new deal being created (no `id` yet) is exempt, since there's nothing to tag yet. The read side (`table-policy.server.ts`) got a matching `isDistributorRole` branch reusing the `scopeOwnerOrParticipantTag` mechanism a prior feature commit (`f511d48`) had already built for `partner_user`'s own narrower "own-or-tagged" visibility — extended with a new `ownerColumn: null` mode so Distributor gets tag-only visibility with no "I created it" fallback at all (unlike `partner_user`, which does get that fallback). Verified: `tsc --noEmit`, `eslint`, and the full test suite all clean (378 pass, the 3 pre-existing unrelated failures confirmed identical on `main`); 4 new tests (2 read-scope in `table-policy.server.test.ts`, 2 write-side in `deal-commands.server.test.ts` covering both the deny-when-untagged and allow-when-tagged cases, plus a guard that `createDeal` never runs the tag check against a not-yet-created deal). Not yet browser-verified end-to-end with a real seeded Distributor account.

**Still open — Task/Ticket:** deliberately not bundled into this fix, since neither `tasks` nor `support_tickets` has its own participant-tag table the way Deals/Customers do — extending "no visibility until tagged" to those two needs a separate design decision (e.g. inherit visibility from a tagged Deal/Customer via `related_id`, or scope to `assignee_id`/`created_by` directly) rather than a copy of this exact mechanism. `authorizeTaskActor`/`authorizeTicketActor` and their read-side scope specs are unchanged — a Distributor still gets flat partner-wide Task/Ticket access.

**Re-verification against product.md's exact text (2026-08-05, same day, second pass):** re-read §8.7/§8.8 in full against the diff above rather than against this document's own paraphrase, and found three more clauses the first pass hadn't covered — two now fixed, one still open:

- **Fixed — the dropdown/lookup bypass.** `listDropdownSourceValues`'s `"client"` source (`dropdown-sources.server.ts`) runs its own raw `pool.query` against `portal_customers` and is used by the manual account/customer pickers, the in-app Assistant, and the WhatsApp wizard's customer browsing — none of which go through `table-policy.server.ts`'s `applyTablePolicy` at all, so the read-scope fix above didn't cover it. A Distributor could still enumerate every customer at their partner tenant through this search box, directly violating §8.8's own acceptance criterion word-for-word: "Distributor cannot discover an untagged Customer." Added the same tag-only `customer_participants` filter here too, threaded a new `isDistributor` field through `DropdownCallerAuth` (caught by the compiler at both existing construction sites — `dropdown-sources.server.ts`'s own `requireAuthenticatedCaller` and `assistant.server.ts`'s `toDropdownCallerAuth`, the latter shared by the WhatsApp wizard — once the field was made required, TypeScript refused to compile until both were updated, which is exactly the point of making it required rather than optional).
- **Fixed — "a Distributor cannot invoke either participant command."** The first pass's tag check in `authorizeDealActor`/`authorizeCustomerActor` means a Distributor already tagged onto a record now *passes* that check — which, combined with `tagDealParticipant`/`untagDealParticipant`/`tagCustomerParticipant`/`untagCustomerParticipant` calling nothing but that same authorize function, meant a tagged Distributor could untag themselves or tag someone else onto a record they're already on — directly contradicting §8.7's explicit "A Distributor cannot invoke either participant command... or retain access merely because a cached card or notification exists." Added a `denyIfDistributor` guard at the top of all four commands, before any tag/authorize check runs, so the role is refused outright regardless of its own tag status. Also added the create-side half of "only an authorised RM in geography scope or PAM in Partner scope may create... a Distributor-to-Customer tag": `denyIfTaggingDistributorWithoutAuthority` blocks any actor other than rm/pam/super_admin from creating a tag with `participantType === "distributor"` (case-insensitive), on both `tagDealParticipant` and `tagCustomerParticipant`.
- **Still open — the "end" half of the RM/PAM-only tagging authority.** `untagDealParticipant`/`untagCustomerParticipant` take only a `participantId`, not the tag's `participant_type`, so enforcing "...or end a Distributor-to-Customer tag" the same way needs a lookup of the existing participant row first to learn what type it is before deciding who may end it — not implemented this pass, tracked as a smaller follow-up rather than rushed in.
- **Still open — the field-level "safe field set."** §8.7's "Distributor Customer access exposes the safe identity, permitted Contacts, shared Tasks, explicitly shared support/shipment context, and explicitly tagged Deals. It excludes unrelated Deals, pricing policy, Partner-wide analytics, and internal notes" is a column/field-level projection requirement, not a record-visibility one — nothing in this fix touches it. A Distributor who *is* correctly tagged onto a Deal today still receives the exact same full field set (pricing, DTP, internal notes) as any other authorised role would. This is the pre-existing, still-open finding already tracked below as **8.7b** (the named `Add/Remove Distributor to/from Customer/Deal` commands with a safe-field-set preview) — re-confirmed accurate, not newly discovered.

Verified again with the same rigor: `tsc --noEmit`, `eslint`, full suite (387 pass, same 3 pre-existing unrelated failures), `bun run build` all clean; 8 new tests in `participant-commands.server.test.ts` (deny-Distributor-outright × 4 commands, deny-non-RM/PAM-tagging-distributor × 2, allow-RM/PAM × 2) plus 1 new test in `dropdown-sources.server.test.ts` asserting the actual SQL text differs by caller (contains `customer_participants`/`valid_to IS NULL` for a Distributor, absent for a Partner Admin).

---

### 2.5 Ch.9 9a (§9.1/§9.2/§9.7/§9.9) — Registration status, pipeline stage, and outcome review are collapsed into one stage field, in three separate code paths — **fixed 2026-08-06 (all three paths)**

**Spec (§9.1/§9.2/§9.7/§9.9):** Registration status, pipeline stage, and outcome review status are three independent dimensions that must never be collapsed into one field. Registration approval (auto or manual) records a decision and unblocks forward movement — it never itself changes pipeline_stage. Forward movement is available only on approved/auto-approved active Deals, one stage at a time, validating each stage's exit/entry criteria.

**Shipped (as of the original audit):** Three separate code paths violate this. (1) submitDealForRegistration (src/server/deal-commands.server.ts:723-734) computes `newStage = autoApproved ? "negotiation" : deal.stage` and writes it directly — clicking "Submit for Registration" on a fresh Sourced deal under $5,000 jumps it straight to Negotiation, skipping Demo/Testing/Qualified/Proposal. (2) admin.deals.tsx's saveReview (src/routes/_authenticated/admin.deals.tsx:272-314, stage logic at 303-310) is a raw client-side `supabase.from("portal_deals").update(...)` call — not routed through any deal-commands.server.ts function at all — that sets `stage: "negotiation"` on Approve, bypassing authorizeDealActor, optimistic-concurrency version checks, and the deal_transitions/domain_activity_events/outbox audit trail entirely. (3) moveDealStageForward (src/server/deal-commands.server.ts:355-376) never inspects deal.status/commercial_approved at all — its resolveTarget only checks FORWARD_NEXT_STAGE[deal.stage] — so a never-submitted Draft deal can be forward-clicked all the way to Negotiation with zero registration gate, and none of §9.8's per-stage minimum requirements (Demo task, Testing plan, Qualified budget, Proposal price snapshot, etc.) or §9.11's post-Testing PAM/KAM handoff/Coverage-Exception rule are checked anywhere in this generic transition.

**Fix (paths 1 and 3, 2026-08-05):** `submitDealForRegistration` no longer computes or writes `newStage` at all — it sets only `status`/`commercial_approved`, leaving `stage` exactly where the submitter left it (§9.1's "registration approval... never itself changes pipeline_stage," literally). `moveDealStageForward` now denies with `"Deal must complete registration approval before moving forward"` whenever `deal.commercial_approved` is false, before even resolving the target stage — matching §9.8's per-stage table, which lists "Approved/auto-approved registration" as a minimum forward requirement starting at Sourced (i.e. applies to every forward move, not only entry into Negotiation). **Not attempted:** the per-stage exit/entry criteria beyond the registration gate (Demo task, Testing plan, Qualified budget, Proposal price snapshot, §9.11's PAM/KAM handoff/Coverage Exception) — those remain unchecked by this generic transition, same as before.

**Fix (path 2, 2026-08-06):** added `reviewDealRegistration` to `deal-commands.server.ts`, right after `submitDealForRegistration`, following the exact same shape: `loadDealForUpdate` (row lock) → `authorizeDealActor(actor, deal, tx)` → an explicit `roleKey !== "super_admin"` gate (admin.deals.tsx has always restricted this whole page to `hasRole("super_admin")`; this command preserves that ceiling rather than widening it — unlike §2.7's PO-review authority, §9.7's registration-review authority was never extended past Super Admin in the shipped UI, so there was no broader existing grant to match) → an optimistic-concurrency `expectedVersion` check → an `UPDATE portal_deals SET status = $1, commercial_approved = $2, version = $3, updated_at = now() WHERE id = $4` that **never references `stage`** → `recordTransitionAndOutbox` with `toStage: deal.stage` (unchanged) and `toStatus` set to the decision. `decision` accepts `"approved" | "need_more_info" | "rejected"` (the three values `REGISTRATION_STATUSES` in `taxonomy.ts` supports that this UI actually offers — `"won"`/`"lost"` were part of the old function's type signature but had no real call site in `admin.deals.tsx`, confirmed by grep, so they were dropped rather than carried forward as dead code). Exposed via a thin `reviewDealRegistration` wrapper in `src/integrations/local/deal-commands.ts`, matching every other command in that file (`resolveActorOrDenial` → dynamic import → call). `admin.deals.tsx`'s `saveReview` now branches: when a `status` decision is passed (the Approve/Request changes/Reject buttons), it calls this command instead of a raw `supabase.from("portal_deals").update(...)`, passing `selectedDeal.version` as `expectedVersion`; the plain "Save changes" button (no decision — general field edits: account/contact/product/amount/etc.) still uses a raw client write as before, but that payload no longer includes `status`/`commercial_approved`/`stage` at all, since those three fields are now exclusively owned by `reviewDealRegistration` — there was never a form control to edit them directly in the "Save changes" path anyway (verified: no Status/Stage field exists in the "Edit review details" grid), so this is a no-op removal, not a capability loss.

**Verification (paths 1 and 3, 2026-08-05):** `tsc --noEmit`, `eslint` clean. 4 tests in `deal-commands.server.test.ts`: `submitDealForRegistration` auto-approves without touching `stage` (asserted directly against the generated SQL, not just the return value), the same for the manual-review-required branch, `moveDealStageForward` denies a never-approved deal at Sourced, and allows one already approved at a later pre-Negotiation stage. Full suite 401 pass (+7 across §2.5+§2.6), same 3 pre-existing unrelated failures, `bun run build` clean.

**Verification (path 2, 2026-08-06):** `tsc --noEmit`, `eslint` clean on all four touched files (`deal-commands.server.ts`, `deal-commands.server.test.ts`, `src/integrations/local/deal-commands.ts`, `admin.deals.tsx`). 6 new tests in `deal-commands.server.test.ts`, reusing the existing `installFakePoolForRegistration` harness (it already discriminates the `portal_deals ... FOR UPDATE` SELECT from other queries and captures UPDATE calls, so no harness changes were needed this time): approve/reject/need_more_info each asserted directly against the generated UPDATE SQL and params (`stage` absent, `status`/`commercial_approved` set correctly per decision — reject and need_more_info both leave `commercial_approved` at its prior value, only approve flips it to `true`), a non-super_admin actor (a `pam` with a global geography ceiling, so it clears `authorizeDealActor`'s own check and hits only the new role gate) is denied with zero UPDATE calls issued, and a stale `expectedVersion` is rejected with `OPTIMISTIC_CONFLICT` and zero UPDATE calls. All 25 pre-existing tests in the file still pass unchanged (31/31 total). `bun run build` — not re-run standalone for this increment; covered by the full end-of-session build/test pass below. Not browser-verified against a real seeded super_admin session clicking Approve/Reject/Request changes end-to-end.

---

### 2.6 Ch.9 9b (§9.15/§9.19) — Won/Lost bypasses the PO/outcome-review flow, permanently orphaning the deal from rewards — **the outcome-review deadlock fixed 2026-08-05; outcome date/PO choice/loss reason fixed 2026-08-06; confirmed-lines/DTP/contributor-split capture still open**

**Spec (§9.15/§9.19):** Selecting Won requires an outcome date, a PO Upload-Now-or-Submit-Later choice, and confirmed final lines/DTP/split before the deal can leave Negotiation; Approve on the outcome-review state machine is what creates the reward award and accounting sync. Ordinary Lost requires a loss reason and is only available from Negotiation.

**Shipped (as of the original audit):** The "Mark won"/"Mark lost" buttons (src/routes/_authenticated/deals.tsx:2474-2497, identical logic in pipeline.tsx:222-269 "Move forward" and deals.tsx:1470-1526 "Advance stage") call closeAs/markDealWon/markDealLost (src/server/deal-commands.server.ts:412-462) unconditionally from any active stage with no reason, no PO, no outcome date, and no confirmed line/DTP snapshot. Once markDealWon sets portal_deals.stage='won', the only PO-submission entry point permanently disappears: DealOutcomeReview's isNegotiation gate (src/components/deal-outcome-review.tsx:33,99) and submitPO's own server check `deal.stage !== "negotiation"` (src/server/outcome-review-commands.server.ts:136) both require the deal to still be in Negotiation. So a deal closed through this (the only reachable) path can never submit a PO, never reach outcome_review_status=approved, and never trigger awardApprovedWinRewards or an accounting sync — the entire §9.15 flow this session's reward fix (awarding on approvePO) depends on is unreachable from the shipped UI. admin.deals.tsx's saveReview("won")/("lost") (admin.deals.tsx:303-310) reproduces the same unconditional bypass via a raw client update.

**Fix (the deadlock, 2026-08-05):** re-read §9.15's own flowchart directly and found the fix is narrower than it first looks: the spec's flow has "Negotiation → Won with Submit Later" and "...with valid PO Upload Now" as the *normal* immediate transitions — reaching stage `"won"` right away is correct and intended, it's not itself the bug. The actual bug is that `submitPO` only ever accepted `deal.stage === "negotiation"`, so the moment a deal became Won it was permanently locked out of ever submitting a PO again — a self-inflicted deadlock, not a missing-capture problem per se. Widened `submitPO`'s stage check to accept `"negotiation"` or `"won"` (a deal can only ever reach `"won"` via `markDealWon`'s own terminal-stage guard, so this doesn't open any new path to submit a PO on a deal that was never negotiated), and widened `deal-outcome-review.tsx`'s PO-submission form visibility (`canSubmitPo = isNegotiation || isWon`, was `isNegotiation` only) to match — a deal marked Won-with-Submit-Later now actually shows the PO form instead of nothing.

**Correction (2026-08-06):** the "`admin.deals.tsx`'s `saveReview("won")`/`("lost")`" clause above (both in the original audit's "Shipped" paragraph and the previous "Still open" note) no longer describes the live code — `saveReview`'s type signature was narrowed to `"approved" | "need_more_info" | "rejected"` only as part of §2.5 path 2's fix earlier in this same session, and a grep of `admin.deals.tsx` confirms it never actually had a "won"/"lost" button call site at the time of this re-check (only Approve/Request changes/Reject are wired) — Won/Lost close on this product exclusively through `deals.tsx`'s `closeAs`/pipeline.tsx's `moveDeal`. Not clear whether admin.deals.tsx briefly had working Won/Lost buttons at the time of the original audit and they were removed by an intervening commit, or the citation was already imprecise; either way it is not a live bypass today.

**Fix (outcome date / PO choice / loss reason, 2026-08-06):** `markDealWon` (`deal-commands.server.ts`) now accepts optional `outcomeDate`/`poChoice: "now" | "later"`; `outcomeDate` overrides the `close_date` write that was previously always hardcoded to today, and `poChoice` is persisted on a new `portal_deals.po_choice TEXT` column (idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, appended at the end of `db/schema.sql` per this file's own ALTER-not-CREATE convention — see the comment directly above it) and also recorded in the `deal_transitions`/`domain_activity_events`/outbox payload via a new generic `extraPayload` hook on `runDealTransitionCommand`. `markDealLost`'s `reason` is now a required (not optional) parameter, validated the same way `moveDealStageBackward` already validates its own required reason (trim, reject blank, fail before the transaction opens) — matching §9.15's "Ordinary Lost requires a loss reason." `deals.tsx`'s one-click "Mark won"/"Mark lost" buttons now open a small dialog first (new `closeDialogStatus`/`closeOutcomeDate`/`closePoChoice`/`closeReason` state): Won shows an outcome-date picker and an Upload-Now/Submit-Later toggle, Lost shows a mandatory reason textarea whose Confirm button is disabled until non-empty. `pipeline.tsx`'s own Won path (triggered implicitly by "Move forward" from Negotiation) was left calling `markDealWon` with no extra fields — still valid since they're optional, but it doesn't get the same capture dialog; noted below as remaining scope, not fixed here.

**Still open:** the confirmed-final-lines/DTP/contributor-split capture §9.15 also requires before a deal can leave Negotiation is unbuilt — it depends on `deal_line_items` actually being populated (9f/18d, addressed later this same session) and is a real, separate, larger piece of work (freezing a Pricing Revision from confirmed lines at the moment of Won), deliberately not attempted here. `pipeline.tsx` has no "Mark lost" UI at all (only `deals.tsx` does) and its "Move forward"-triggered Won path doesn't show the new outcome-date/PO-choice dialog — out of scope for this pass since the task was to fix the existing "Mark lost" call sites, not add a new one.

**Verification (deadlock, 2026-08-05):** `tsc --noEmit`, `eslint` clean. 3 tests in `outcome-review-commands.server.test.ts`: `submitPO` accepted from negotiation (regression-proves the existing behavior still works), accepted from won (proves the deadlock is closed), still denied from an unrelated stage like qualified (proves this isn't a blanket stage-check removal). Full suite 401 pass, same 3 pre-existing unrelated failures, `bun run build` clean.

**Verification (outcome date/PO choice/loss reason, 2026-08-06):** `tsc --noEmit`, `eslint` clean on all touched files (`deal-commands.server.ts`, `deal-commands.server.test.ts`, `src/integrations/local/deal-commands.ts`, `deals.tsx`, `db/schema.sql`). 4 new tests in `deal-commands.server.test.ts`: `markDealLost` rejected with a blank/whitespace-only reason (zero UPDATE calls issued), `markDealWon` records a supplied `outcomeDate`/`poChoice` directly in the generated UPDATE params (not just the return value), and falls back to today's date plus a null `po_choice` when neither is supplied. Full deal-commands suite: 34/34 pass. Not browser-verified against a real seeded session clicking through the new dialog.

---

### 2.7 Ch.9 9c (§9.7/§9.15) — PO approval buttons are shown to PAM/RM but the server accepts only literal super_admin — **fixed 2026-08-05**

**Spec (§9.7/§9.15):** The PO/outcome-review state machine authorizes "Tagged PAM/RM or Super Admin" to approve, request changes on, or reject a claimed Won outcome.

**Shipped (as of the original audit):** deal-outcome-review.tsx:27 sets `isSuperAdmin = access.isLiveyInternal`, which is true for rm, pam, kam, isr, livey_support, and restricted_distributor (src/domain/contracts/features.ts:67-75), and renders the full Approve/Request-changes/Reject panel (deal-outcome-review.tsx:139-169) to all of them. But the server commands they call — approvePO (src/server/outcome-review-commands.server.ts:205-211), requestChanges (247-253), rejectOutcome (283-289) — each hard-reject with `roleKey !== "super_admin"`. Every tagged PAM or RM sees fully-enabled buttons that fail every time with "Only super_admin can approve POs"/etc. Separately, none of these three commands call authorizeDealActor at all (only the literal role check), so if the role check is ever loosened to admit pam/rm without also adding deal/partner/geography scoping, any pam or rm system-wide — not just ones tagged to this specific deal — could approve or reject any deal's outcome (the same vulnerability class already fixed this session for approveDiscount in pricing-commands.server.ts).

**Fix:** All three commands now call `authorizeDealActor(input.actor, deal, tx)` first (closing the scope hole — a pam/rm outside their geography ceiling is rejected exactly like every other deal command), then check role membership against a new `PO_REVIEW_ROLES` set (`super_admin`/`pam`/`rm`) via `authorizePoReviewer`. The full participant-tag model ("Tagged PAM/RM") isn't built yet (§5.7/§9g, still open — automatic PAM/KAM tagging doesn't exist), so `authorizeDealActor`'s existing geography/partner scoping stands in as the interim substitute for "tagged," the same deliberate trade-off already used for `isLiveyInternalRole` elsewhere in `table-policy.server.ts`. The UI (`deal-outcome-review.tsx`) was narrowed to match: `isSuperAdmin` is now `roleKey === "super_admin" || "pam" || "rm"` (was `access.isLiveyInternal`, which also covered kam/isr/livey_support/restricted_distributor — those roles no longer see a panel that's guaranteed to fail for them), and the panel heading changed from "Super Admin Actions" to "Outcome Review Actions" since it's no longer super_admin-exclusive.

**Verification:** `tsc --noEmit`, `eslint`, full suite (394 pass, same 3 pre-existing unrelated failures) all clean; `bun run build` clean. 4 new tests: a PAM inside their geography scope is now allowed (previously always failed), an RM *outside* their geography scope is still denied (proves this isn't a blanket role grant), a KAM is still denied even in scope (not a named PO-review role), and `requestChanges`/`rejectOutcome` both accept a scoped RM the same way `approvePO` does. The 5 pre-existing tests (including the one asserting `partner_admin` is denied) all still pass unchanged. Not browser-verified end-to-end against a real seeded PAM/RM account — same open item as the rest of today's server-side RBAC work.

---

### 2.8 Ch.9 9d (§9.5/§9.13/§9.19) — Pipeline value wrongly includes Won/Lost deals, summed off the free-text amount instead of DTP — **dashboard half fixed 2026-08-02; Pipeline-page headline total fixed 2026-08-06; card field/label gaps still open**

**Spec (§9.5/§9.13/§9.19):** Open pipeline value must be scoped to Sourced-through-Negotiation only, computed from quantity × current effective DTP; Won and Lost are excluded from open/weighted pipeline. Every pipeline card must show Partner, Customer, POC, a stage-specific value label (Pipeline value / Claimed won value pending review / Won value / Lost value), local reference, next task, RM/ISR/PAM/KAM participant chips, and a registration/outcome-review indicator, with Move forward and Move backward both available as card actions.

**Shipped:** pipeline.tsx's `totals.pipeline` (src/routes/_authenticated/pipeline.tsx:209-212) sums `getDealUsdAmount(deal)` — which reads the free-text `amount`/`amount_usd` column (src/lib/portal-records.ts:283-289), never deal_line_items/pricing_revisions DTP — over `regionScopedDeals`, which is only region-filtered (pipeline.tsx:181-184), not stage-filtered, so Won and Lost deals are included in the headline "pipeline" figure every time the page loads. The board cards (pipeline.tsx:453-503) show only account name, owner name, region, the single `product` string, probability, and a flat `deal.amount` badge (line 464) — no Customer, no POC, no stage-specific value label, no local reference, no next task, no participant chips, no registration/outcome indicator — and offer only "Move forward"/"Notes" (lines 482-498), with no "Move backward" action anywhere on the card.

**Correction (2026-08-05, re-verified against current code):** commits `e0e1696`/`152b433`/`a350cb4` (2026-08-02, "define canonical open pipeline metric" / "scope dashboard pipeline aggregation" / "report open pipeline from effective dtp") gave `dashboard-metrics.server.ts` a real server-side canonical open-pipeline figure computed from effective DTP — that half is fixed. But this is a *different* number from the one this finding is actually about: the Pipeline *page*'s own headline `totals.pipeline` (`pipeline.tsx:190-201` as of this re-check) is untouched — still `regionScopedDeals.reduce((sum, deal) => sum + getDealUsdAmount(deal), 0)`, still summing the free-text amount over every stage including Won/Lost, since `regionScopedDeals` (line 162) filters only by region, never by stage. The dashboard KPI and the Pipeline page's own total are two separate, redundant calculations of "pipeline value" today — one now correct, one still not — which is itself worth fixing as part of closing this out (Pipeline page should read the same canonical metric the dashboard now computes, not maintain its own parallel client-side sum).

**Fix (headline total, 2026-08-06):** `pipeline.tsx`'s `load()` now calls the exact same `loadDashboardPipeline(selectedRegion)` server function the dashboard already uses (`src/integrations/local/dashboard-metrics.ts` → `loadDashboardPipelineMetrics` → the shared `summarizeOpenPipeline`/`isOpenPipelineStage` helpers in `src/lib/pipeline-metrics.ts`), in parallel with the existing deal/collaborator queries, and stores the result in a new `pipelineMetric` state. The "Pipeline value" `MetricCard` now renders `pipelineMetric.pipelineValueUsd` (falls back to "Unavailable" if the call fails) instead of the old client-side `regionScopedDeals.reduce(..., getDealUsdAmount)` sum — so Won/Lost deals are excluded and the figure is priced from effective DTP (falling back to `amount_usd`/USD-only `amount_value`, never the free-text `amount`), identically to the dashboard KPI. This is the third-duplicate-logic concern the correction above called out: there is now exactly one canonical implementation (`pipeline-metrics.ts`), reused by both surfaces, not a second parallel client-side calculation. `getDealUsdAmount`/its import were removed from `pipeline.tsx` since nothing else in the file used it. "Deal count"/"Avg. probability" cards are unchanged (they were never part of this finding — they intentionally still describe the whole visible, region-scoped queue, not just open-stage deals).

**Still open:** the board-card field/label gaps this finding also names — Customer, POC, stage-specific value label ("Pipeline value" / "Claimed won value pending review" / "Won value" / "Lost value" per card instead of one flat `deal.amount` badge), local reference, next task, and RM/ISR/PAM/KAM participant chips are still absent from the pipeline board cards (`pipeline.tsx`'s card renderer). "Move backward" as a card action is addressed separately in this same session's 9e work (see the Chapter 9 table's 9e row) since it applies to the deal-detail surface too, not just this card.

**Verification:** `tsc --noEmit`, `eslint` on `pipeline.tsx` both clean. `bun test src/lib/pipeline-metrics.test.ts src/server/dashboard-metrics.server.test.ts` (9/9 pass) — the reused logic already had full coverage; no new server logic was added by this change, only a new call site, so no new test file was needed. Not browser-verified against a live dev server in this pass.

---

### 2.9 Ch.10 10a (§10.2) — Tasks can be updated directly through the generic table path, bypassing the transition guard entirely — **fixed 2026-08-02 (never cross-referenced back to this document until this re-verification)**

**Spec (§10.2):** "A workflow may propose the next valid transition but cannot update Task status by direct field write." All status changes must go through the governed transition command (mandatory reasons for blocked/cancelled/reopen, allowed-transition checks, actor/time capture, Activity history).

**Shipped (as of the original audit):** transitionTask (src/server/task-commands.server.ts:296-392) correctly enforces the state machine, reason requirements, and writes task_transitions/domain_activity_events/outbox records. But that command is not the only writable path: the generic table-policy update executor (src/server/livey-service.server.ts:1084-1113, reached via supabase.from("tasks").update(...) -> queryTable) filters only by a column-name allowlist, with zero business-rule or field-level restriction, and its row-scope check (src/server/table-policy.server.ts:742-754, getScopeSpec case "tasks" at 406-414) only matches partner_id/creator_id, not which columns are being set. role_permissions grants can_update=true for the "tasks" feature to super_admin, rm, pam, kam, isr, livey_support, partner_admin, partner_user, and restricted_distributor (db/schema.sql:1414-1526). Any of these roles can therefore call `supabase.from("tasks").update({status:"cancelled"})` (or completed/to_do/etc.) directly on a task in their scope, skipping mandatory reasons, allowed-transition checks, optimistic-concurrency version checks, and the entire task_transitions/outbox audit trail that transitionTask writes.

**Correction (2026-08-05, found while attempting to fix this):** this was already fixed — `table-policy.server.ts`'s `applyTablePolicy` calls `assertNoProtectedLifecycleUpdate(query)` before anything else runs, which throws `"Task lifecycle fields require the named transition command"` whenever a generic `tasks` update touches `status`, `version`, `blocked_reason`, `completed_at`, or `cancelled_at` — the exact five columns `transitionTask` itself writes — with no super_admin bypass, matching the spec's absolute (not role-scoped) framing. This landed in commit `bef8ee5` (2026-08-02, "real Insight Hub completion flow, full-database Assistant, auto-task-on-tag" — an unrelated feature commit) and was simply never cross-referenced back to this entry. A first attempt at "fixing" this independently duplicated the exact same guard under a different name before the pre-existing one was noticed mid-implementation (its error message didn't match what the new code expected — that mismatch is what surfaced it); the duplicate was reverted and 3 regression tests were added against the real, pre-existing guard instead (`table-policy.server.test.ts`). Ordinary task columns (title, description, priority, due_at, assignee_id) remain editable through the generic path, as they should.

---

### 2.10 Ch.10 10b (§10.3) — "My Tasks" scopes by creator, not assignee — tasks assigned to you are invisible on your own page — **fixed 2026-08-01**

**Spec (§10.3):** "My Tasks" is the primary task workspace, built around tasks assigned to the current user (Today/Upcoming/Overdue/Blocked/Assigned by Me/Completed).

**Shipped:** The read-scope filter for the "tasks" table (getScopeSpec, src/server/table-policy.server.ts:406-414) is `partner_id = actor's partner` when the actor has a partnerId, else `creator_id = actor's userId` — assignee_id is never used as a scope column anywhere in table-policy.server.ts (confirmed by grep). tasks.tsx's only query (src/routes/_authenticated/tasks.tsx:84-99, `supabase.from("tasks").select("*")`) relies solely on this scope. Consequence: for every LIVEY-internal role without a partnerId (rm, pam, kam, isr, livey_support — i.e. everyone except super_admin, who bypasses scope entirely at table-policy.server.ts:710-712), a task assigned to them by someone else (a different creator_id) is completely invisible on their own "My Tasks" page; they only ever see tasks they personally created. This breaks the core premise of the page for the exact roles the blueprint's stage-generated tasks target (e.g. "acknowledge PAM/KAM assignment" in §10.4's table).

**Fix:** A single ownership column can't express "creator OR assignee", so added a new, narrowly-scoped mechanism (`scopeAnyColumnEquals` on the query object, purpose-built rather than a generic raw-SQL escape hatch) that the policy layer sets for LIVEY-internal `tasks` reads and the SQL-building layer in `livey-service.server.ts` turns into `("creator_id" = $1 OR "assignee_id" = $1)`. `tasks.tsx` needed no client-side change — unlike deals/support, it has no `applyPartnerScope` client-side filter, so the server fix alone was sufficient. Verified end-to-end: created a real governed `rm` test user locally, assigned them a task created by a different user, confirmed it appeared under "My Tasks" (previously invisible).

---

### 2.11 Ch.11 11a (§11.5/§11.6) — Notifications target the acting user or the whole partner org, never the record's actual assignee/watchers

**Spec (§11.5/§11.6):** Notification recipient rules require reconciling the event's typed participants (assignee, requester, watchers, tagged roles), removing the actor where self-notification is unnecessary, and creating one recipient-specific in-app notification per user. The blueprint explicitly states: "Notifications do not target all members of a Partner merely because partner_id matches." Required tagged notifications include ticket reply/assignment/SLA escalation/closure/reopen and task assignment/blocking/overdue/completion.

**Shipped:** recordNotification/raw inserts (src/lib/workflow-events.ts:13-34, src/routes/_authenticated/pipeline.tsx:141-165, src/routes/_authenticated/deals.tsx:872-915) set user_id to the ACTOR performing the action, not the deal's assignee/watchers. Both server-side scoping (src/server/table-policy.server.ts:230-234 buildGenericScope and :365-368 getScopeSpec, case "notifications": scope column is "partner_id" first) and client reads (src/routes/_authenticated/notifications.tsx:43-52 applyPartnerScope, src/components/app-shell.tsx:71-81 unread-count query) filter by partner_id only — so every user at the same partner sees, and "Mark all as read" (notifications.tsx:78-91) marks read, every other teammate's personal notifications. For Livey-internal roles the bell's unread count query (app-shell.tsx:79-81) drops partner scoping entirely and counts every unread notification system-wide. Separately, ticket-commands.server.ts and task-commands.server.ts (grepped for "notif", zero matches) never create a notification for ticket reply/assignment/SLA escalation/closure/reopen or task assignment/blocking/overdue/completion — those required tagged notifications never fire at all; only ad-hoc client code for deals/partners/rewards creates any notification.

---

### 2.12 Ch.11 11b (§11.1/§11.4) — Deal activity is written into the global News feed, leaking one partner's deal details to every partner — **fixed 2026-08-02**

**Spec (§11.1/§11.4):** "News and Activity never share a storage model merely because they appear beside each other on a dashboard," and the dashboard feed must "never leak a count or preview from an unauthorised record."

**Shipped (as of the original audit):** publishDealActivity (src/routes/_authenticated/deals.tsx:872-915, src/routes/_authenticated/pipeline.tsx:141-179) inserts a row into portal_news_posts for every deal stage change/note/won-lost/submission, embedding the customer account name and product (e.g. deals.tsx:1502-1508, admin.deals.tsx:320-326 `${selectedDeal.account_name} was marked ${status} by admin`). portal_news_posts is in PUBLIC_READ_TABLES (src/server/table-policy.server.ts:61-65) with no partner/audience scoping on select, and dashboard.tsx:185 and partner.tsx:126 both do an unfiltered `select("*")`, so any authenticated user of any partner sees every other partner's deal account names/products/stage changes on their News tab. This path is only reachable in practice via super_admin actors (admin.deals.tsx): the identical insert attempted by ordinary partner_admin/partner_user callers in pipeline.tsx/deals.tsx is rejected by table-policy.server.ts:618-621 (`isWrite && !superAdmin => Access denied`) and silently swallowed by the unchecked `Promise.allSettled` (deals.tsx:892-915, pipeline.tsx:155-178) — so for ordinary partner-triggered deal moves the "activity feed" entry silently never appears, while for super-admin-triggered deal actions it leaks the partner's deal details globally.

**Fix (found already applied, 2026-08-05 re-verification):** commit `7b99e48` (2026-08-02, "separate workflow activity from editorial news") removed every `portal_news_posts` insert from deal-lifecycle code — confirmed by grep, zero references remain in `deals.tsx`, `pipeline.tsx`, `admin.deals.tsx`, or `admin.partners.tsx`. Deal stage/registration changes keep their Notification/Activity/audit evidence; `admin.news.tsx` is now the one explicit editorial publishing surface, exactly as the spec quote requires. A dedicated regression test exists (`src/lib/news-activity-boundary.test.ts`).

---

### 2.13 Ch.13 13a (§13.2/§13.3/§13.10) — Ticket creation inserts into two columns that don't exist in the deployed schema — every real ticket create fails — **fixed 2026-08-02**

**Spec (§13.2/§13.3/§13.10):** Ticket creation supports optional product rows and serial numbers per product; acceptance criteria require every ticket to actually be creatable and usable.

**Shipped (as of the original audit):** createTicket() in src/server/ticket-commands.server.ts:262-280 unconditionally INSERTs into support_tickets.product_sku and support_tickets.serial_number, but neither column exists anywhere in db/schema.sql — the only file actually applied to the database via `bun scripts/apply-migrations.ts` / `npm run db:migrate` (confirmed by grepping the whole 1703-line file for both names). Those two columns were only ever defined in the disconnected supabase/migrations/20260730000000_phase4_tickets.sql (lines 1-2), whose sibling ALTER for support_ticket_comments.is_internal (line 3 of that same file) was ported into db/schema.sql:1642 this session (with an explicit comment explaining the is_internal bug) — but the product_sku/serial_number siblings from that identical migration were never ported. Every invocation of createTicket (the sole ticket-creation path, wired from support.tsx's "Create ticket" button at line 380) throws a Postgres "column does not exist" error and fails 100% of the time. Seed scripts don't hit this (scripts/seed-phase1-supplement.ts inserts tickets directly without those columns), which is why pre-seeded demo tickets render fine while real user-driven creation is completely broken.

**Fix (found already applied, 2026-08-05 re-verification):** `db/schema.sql:1217-1218` now has `ALTER TABLE support_tickets ADD COLUMN IF NOT EXISTS product_sku TEXT;` / `... serial_number TEXT;`, landed in commit `0b03c62` (2026-08-02, "align canonical ticket and reward schema for repeatable deploys" — a dedicated schema-repair commit prompted by a real reproduced deploy failure, per its own commit message). `createTicket`'s INSERT now matches the deployed schema.

---

### 2.14 Ch.13 13b (§13.5) — livey_support sees no ticket action controls at all — a UI-only lockout of its own primary role — **fixed 2026-08-02**

**Spec (§13.5):** LIVEY Support and Super Admin can close a Ticket (and by extension accept/wait-on-partner/decide-reopen — the same joint authority the command layer enforces).

**Shipped (as of the original audit):** src/routes/_authenticated/support.tsx:524 gates the entire accept/wait-on-partner/close/reopen-decide action panel (and the priority/assignee edit panel) behind `hasRole("super_admin")` only — there is no `hasRole("livey_support")` branch anywhere in that block (grep confirms livey_support is only checked for comment visibility at lines 127/262 and the internal-note checkbox at line 718). Meanwhile src/server/ticket-commands.server.ts's SUPPORT_ROLES/requireSupportRole (lines 56, 123-132) correctly authorizes both super_admin and livey_support at the command layer. The result is a UI-only lockout: a livey_support user, viewing an open ticket, sees no accept/close/wait controls at all (the ternary's else-branch only renders a partner-facing "Request reopen" button, and only when status is closed), so the role the blueprint names as primary ticket-closing authority can never actually manage a ticket through the product's own interface.

**Fix (found already applied, 2026-08-05 re-verification):** commit `60d52e5` (2026-08-02, "expose ticket workflow to livey support") added `src/lib/ticket-permissions.ts`'s `canManageTicket(roleKey)`, which now gates the whole action panel (`support.tsx`'s `canManageSelectedTicket`) and includes both `super_admin` and `livey_support`, with a comment noting it "mirrors `SUPPORT_ROLES` in `ticket-commands.server.ts` exactly." The priority/assignee edit sub-panel (`saveTicketPriority`, still gated to `hasRole("super_admin")` only at the time of this re-check) was not covered by this fix and remains a narrower, separate presentation-layer gap — not the primary lockout this finding was about, which is resolved.

---

### 2.15 Ch.15 15a (§15.1/§15.4/§15.7) — Redemptions never reserve points or stock; approval revalidates nothing and can double-spend — **fixed 2026-08-02**

**Spec (§15.1/§15.4/§15.7):** Pending redemptions must reserve points atomically; approval/fulfillment must revalidate balance, eligibility, and stock; ledger must carry Redemption Reservation / Reservation Release / Redemption Fulfillment Debit events; and the full Requested→Points Reserved→Pending Review/Processing→Fulfilled/Failed/Refunded state machine must be enforced with stock/points effects exactly-once.

**Shipped (as of the original audit):** submitRequest (src/routes/_authenticated/rewards.tsx:226-272) only inserts a reward_redemptions row with status 'requested' — no reward_point_events reservation row is created, no stock check, no balance re-check beyond a stale client-side `points >= cost` gate. approveRedemption (src/routes/_authenticated/admin.rewards.tsx:357-412) makes two independent, non-transactional supabase calls (update redemption status, then insert a negative points_delta ledger row) with no check that the user's current balance actually covers it and no decrement of reward_catalog_items.stock anywhere in the codebase. rejectRedemption never needs to 'release' anything because nothing was ever reserved. Only 3 ad hoc statuses exist (requested/approved/rejected) versus the 10+ states/transitions required. table-policy.server.ts's scope rules for reward_redemptions/reward_point_events (lines ~229,364) only add partner/user row-scoping — no business-rule enforcement exists anywhere for this table.

**Fix (found already applied, 2026-08-05 re-verification):** a real `src/server/reward-commands.server.ts` (836 lines, 1291 lines of tests) now exists, landed across commits `ac2625e`/`0e4e91c`/`f34e66f` (2026-08-02, "add reward reservation integrity schema" / "reserve, review, release, and retire rewards atomically" / "route reward mutations through commands"). `requestRewardRedemption` takes `FOR UPDATE` row locks on the profile and catalog item, checks an idempotency key, and inserts a real `redemption_reservation` ledger event; `approveRewardRedemption`/`rejectRewardRedemption` look up and release/consume that exact reservation row rather than trusting client state; `rewards.tsx`/`admin.rewards.tsx` were rewired to call these commands instead of raw `supabase.from(...)` writes. Concurrency behaviour is covered by a fake pool with a real per-row async lock in the test suite, not just stubbed responses. Still not verified against the full 10+-state canonical model from the spec (states used are a smaller practical set), but the core "never reserve, can double-spend" defect this finding is about is resolved.

---

### 2.16 Ch.15 15b (§15.1/§15.3) — Any deal creator (LIVEY-internal or Distributor) can be awarded 100% of Won rewards with no eligible partner contributor — **fixed 2026-08-05**

**Spec (§15.1/§15.3):** LIVEY internal roles and Distributor must never receive Partner reward points; if a LIVEY user (or Auto CRM) created the Deal and no eligible Partner contributor exists, Won approval must be blocked until one is assigned.

**Shipped (as of the original audit):** awardApprovedWinRewards (src/server/outcome-review-commands.server.ts:64-89) falls back to `[{ userId: deal.user_id, splitPercent: 100, sortOrder: 0 }]` whenever portal_deal_collaborators is empty, with no check on the creator's role. Since rm/pam/kam/isr and restricted_distributor can all create deals (db/schema.sql:1423,1437,1451,1465,1493 — create=true), and approvePO (outcome-review-commands.server.ts:193-234) never blocks on missing eligible contributors, a LIVEY or Distributor deal creator who never tags a partner collaborator is awarded 100% of the deal's reward points instead of Won approval being blocked.

**Fix:** added `hasEligiblePartnerContributor(tx, dealId)` — one query checking whether the deal's creator (`portal_deals.user_id`) or any tagged `portal_deal_collaborators` row belongs to a real `partner_admin`/`partner_user`. `approvePO` now calls this before doing anything else and returns a validation failure (no `deal_outcome_reviews`/`portal_deals` update, no reward insert — verified directly against the update-call log, not just the return value) whenever it's false, matching §15.3's literal text: "Won approval is blocked until an eligible contributor is assigned." This blocks the whole approval, not just the reward — an ineligible-creator deal can't reach Approved Won at all until a real Partner contributor is tagged.

**Verification:** `tsc --noEmit`, `eslint` clean. 2 new tests (blocks with no eligible contributor and awards nothing; proceeds normally when one exists). Had to extend the shared `installFakePool` test harness in the same file: the new query's SQL text contains `FROM portal_deal_collaborators` as a substring (inside a nested subquery), which the harness's existing generic matcher would have intercepted first and silently returned `[]` for every pre-existing test that doesn't explicitly set `collaborators` — added a more specific, earlier-checked branch plus a `hasEligiblePartnerContributor` test option defaulting to `true`, so none of the 12 pre-existing tests needed to change and all still pass. Full suite 403 pass (+2), same 3 pre-existing unrelated failures, `bun run build` clean. Not browser-verified.

---

### 2.17 Ch.16 16a (§16.2) — Server ignores requested column allowlists on read — the profiles export leaks every user's password_hash — **fixed 2026-08-01**

**Spec (§16.2):** Exports must apply row- and field-level authorisation and exclude secrets and sensitive system fields.

**Shipped:** The profiles governance export (src/lib/export-registry.ts:355-376, scopeMode 'global') and every other read of the `profiles` table only restrict which columns are rendered in the CSV client-side (buildCsv only writes the allow-listed `columns`) — but the underlying fetch is never actually column-scoped. QueryBuilder.select(columns) in src/integrations/local/client.ts:303-306 just stores the string on the query state; the server (src/server/livey-service.server.ts:996-1029, `queryTableWithAuthContext`) always executes `SELECT * FROM ${quoteIdent(policyQuery.table)}...` (line 1023) and never reads `policyQuery.select` anywhere in that file or in table-policy.server.ts. `TABLE_COLUMNS.profiles` (livey-service.server.ts:74-88) explicitly includes `password_hash`, and `serializeDbValue` (lines 939-958) only converts Dates/Buffers, performing zero field redaction. Concretely, admin.users.tsx:183-190 explicitly calls `.select("id, email, full_name, phone, company_name, partner_id, partner_status, is_seed, created_at")` (deliberately omitting password_hash) yet the server ignores that column list and returns the full row, including password_hash, in the network response for every profile in the list — and a super_admin clicking "Export CSV" on the profiles governance dataset materializes every user's password_hash into the browser's memory/network traffic system-wide in one request.

**Fix:** `queryTableWithAuthContext` now honors an explicit `.select(...)` narrower than `"*"`, validating every requested column against the table's existing `TABLE_COLUMNS` allowlist and building an explicit `SELECT "col1", "col2", ...` instead of always running `SELECT *`. The default (`.select("*")` or no `.select()` call at all) is unchanged, so no caller that relies on getting every column back can silently lose one. While tracing this, found a second, more pervasive instance of the same bug class: `getAuthContext()` — called on every authenticated page load — separately ran its own raw `SELECT id, email, password_hash, ...` and returned the full row (including `password_hash`) as part of the payload sent to the browser on every request, for every user's own account, not just via the admin export. Fixed by dropping `password_hash` from that query entirely (nothing legitimately reads it from the returned shape — the actual credential check in `signInWithPassword` uses its own separate raw query). See `docs/implementation-status.md`'s 2026-08-01 session for verification detail (proved both bugs via reverted-fix/failing-test/restored-fix, then confirmed end-to-end in a real browser against seeded prod-shaped data that `admin.users.tsx`'s user list and `getAuthContext`'s bootstrap payload no longer contain `password_hash`).

---

### 2.18 Ch.16 16f (§16.5) — Reward catalog items hard-delete with no confirmation, cascading away redemption history — **fixed 2026-08-02**

**Spec (§16.5):** Configuration cannot delete a value referenced by history; it must be retired instead.

**Shipped (as of the original audit):** src/routes/_authenticated/admin.rewards.tsx's deleteItem (lines 307-325) hard-deletes a `reward_catalog_items` row with a single click and no confirmation dialog (no AlertDialog/window.confirm anywhere in the file) and no check for existing redemption history. db/schema.sql:700 defines `reward_redemptions.reward_id UUID NOT NULL REFERENCES reward_catalog_items(id) ON DELETE CASCADE`, so deleting a reward that has ever been redeemed permanently destroys every historical `reward_redemptions` row for it (the points-liability/redemption history required as an analytics report group in §16.1). table-policy.server.ts's rule for this table (lines 892-901: `reward_catalog_items` — "if (!superAdmin && isWrite) throw... return {...query, filters}") imposes no restriction beyond role, i.e. it permits the delete outright rather than forcing a retire-only path.

**Fix (found already applied, 2026-08-05 re-verification):** part of the same reward-commands rebuild as §2.15. `admin.rewards.tsx` now calls `retireRewardCatalogItem` (from `reward-commands.server.ts`) instead of a hard delete — confirmed live at the call site (`admin.rewards.tsx:317`). Redemption history and ledger rows are preserved; a retired item stops accepting new requests rather than being destroyed.

---

### 2.19 Ch.17 17a (§17.3, §17.4.1, §17.1 rule 9) — Zoho Sign send-agreement/resync-agreement endpoints have zero server-side auth check — **fixed 2026-08-01**

**Spec (§17.3, §17.4.1, §17.1 rule 9):** Only Super Admin may select/upload an agreement and send it, and every integration action requires 'explicit integration action permission' scoped to a capability — the interface is capability-scoped and every provider-triggering action is authorised.

**Shipped:** handleZohoSendAgreement and handleZohoResyncAgreement in src/server/zoho-api.server.ts (lines 250-351 and 353-429) perform zero session/role check anywhere in the function body — they trust `partnerId` straight from the request body and proceed to call the real Zoho Sign API (sendAgreement) or mutate the partner's status. The only gating is client-side: admin.partners.tsx:544 does `if (!hasRole("super_admin"))` before rendering the page, and calls these routes from fetch("/api/integrations/zoho-sign/send-agreement", ...) at admin.partners.tsx:411 and 436. The routes are wired with no wrapper in src/server.ts:72-77. By contrast, handleZohoSignUrl in the same file (lines 431-491) does call getAuthContext(sessionToken) and returns 401 without a valid session — proving the auth path exists in this file but was simply omitted for send-agreement/resync-agreement. Any caller who knows or guesses a partnerId can POST directly to these endpoints (bypassing the UI) to send a real signing request to a partner's owner email or flip partner status via resync, with no credential at all.

**Fix:** added a shared `requireSuperAdminSession(request)` check (same cookie-extraction + `getAuthContext` pattern `handleZohoSignUrl` already used) to both handlers, returning 401 for no session, an invalid/expired session, or a session that isn't `super_admin`.

---

### 2.20 Ch.17 17b (§17.1 rules 4 & 6, §17.2 (Webhook receipt), §17.10 bullet 3) — Webhook signature verification is skipped by default, and no durable receipt is ever persisted — **partially fixed 2026-08-01**

**Spec (§17.1 rules 4 & 6, §17.2 (Webhook receipt), §17.10 bullet 3):** Every inbound webhook must be signature/timestamp/account/replay verified before processing, and a durable Webhook receipt must be persisted before domain processing begins; an invalid signature must produce no domain action.

**Shipped:** verifyZohoWebhookSignature in src/lib/zoho-sign.ts:409-420 contains `if (!webhookSecret) return true; // skip verification if not configured` — if ZOHO_SIGN_WEBHOOK_SECRET is unset the signature check is bypassed entirely and every payload is treated as valid. That env var is documented as `# Optional: HMAC secret for webhook signature validation` in .env.example:15, i.e. the app is designed to run with verification disabled by default. Even when the secret is set, only an HMAC-equality check runs (src/lib/zoho-sign.ts:415-419) — there is no timestamp, replay-window, or provider-account check. handleZohoWebhook (src/server/zoho-api.server.ts:97-145) also never writes a durable receipt: the `command_inbox` table exists in db/schema.sql:785-799 with a UNIQUE(source, source_message_id) constraint built exactly for this, but grep across src/ shows zero references to command_inbox from any webhook handler — the Zoho webhook goes straight from signature check to domain mutation (markPartnerSignedPendingReview) with no receipt record at all. This is the item flagged in the audit as 'bypasses policy — still true' and is confirmed unchanged this session.

**Fix (partial):** `verifyZohoWebhookSignature` now fails closed (returns `false`, not `true`) when no secret is configured, and uses a timing-safe buffer comparison instead of `===`. A random secret was generated and set as `ZOHO_SIGN_WEBHOOK_SECRET` in Railway. **This does not make the integration fully secure yet** — Zoho Sign's own webhook configuration (an external dashboard this session has no credentials for) must be updated to sign outgoing webhooks with the *same* secret value, or every real webhook call will now be correctly-but-uselessly rejected with 401. Until that one manual step happens on the Zoho side, the "Resync" button in `admin.partners.tsx` (which polls Zoho directly via `handleZohoResyncAgreement`, now also fixed to require super_admin) is the working fallback for pulling a signed agreement's status. Timestamp/replay-window/provider-account verification and the durable `command_inbox` receipt are still unbuilt — not attempted in this pass.

---

### 2.21 Ch.17 17c (§17.3 (Integration Operations Centre card fields and operator actions)) — Integration Operations Centre is entirely fabricated — no operator action touches a real provider

**Spec (§17.3 (Integration Operations Centre card fields and operator actions)):** Super Admin's Integration Operations Centre must show each provider's real connection state, queue depth, dead-letter counts, conflicts, and support real pause/resume/reconnect/disconnect operator actions with guarded state transitions.

**Shipped:** src/routes/_authenticated/admin.integrations.tsx is entirely fabricated: INITIAL_PROVIDERS (lines 54-133) is a hardcoded array of fake state/queue/dead-letter/conflict numbers for all 6 providers ('2 mins ago', queueDepth: 0, conflicts: 2, etc. — no query anywhere populates these). handleAction (lines 182-212) does `await new Promise((resolve) => setTimeout(resolve, 800))` to simulate a network call, then only calls `setProviders(...)` to mutate local React state and shows a toast — Pause/Resume/Disconnect/Reconnect/'Reconcile All' perform no backend request whatsoever. A super_admin who clicks Disconnect on a broken WhatsApp integration believes they have stopped outbound work; nothing has changed on any real provider connection, queue, or credential.

---

### 2.22 Ch.18 18e (§18.8 (Audit versus Activity — Activity audience)) — Activity Timeline is super-admin-only and silently renders empty for every other role — **partially fixed 2026-08-01**

**Spec (§18.8 (Audit versus Activity — Activity audience)):** Table §18.8 explicitly differentiates Audit Event audience ('Privileged and scope-authorised auditors') from Activity Event audience ('Users authorised to see the subject') — an Activity Event must be visible to anyone who can see the record it's attached to, not just privileged auditors.

**Shipped:** src/server/table-policy.server.ts lines 884-892 gates every select/count on domain_activity_events (the Activity Event table) behind `if (!superAdmin) throw new Error('Access denied')`, with the code's own comment acknowledging this is a placeholder ('until it lands, apply the same super-admin-only reads... rather than ship an under-scoped rule'). src/components/deal-activity-timeline.tsx (rendered on every deal detail page, deals.tsx:2423) queries this table directly via the generic client path (`supabase.from('domain_activity_events')`, lines 28-34) and silently swallows the resulting Access-Denied error into an empty list (`catch { setEvents([]); }`). src/routes/_authenticated/dashboard.tsx lines 189-193 does the same for the dashboard's recent-activity feed, also silently absorbed via Promise.allSettled. Net effect: the Activity Timeline and dashboard activity feed are completely non-functional (permanently empty, no error shown) for every role except super_admin — including partner_admin/partner_user viewing their own deal, and every internal rm/pam/kam/isr/livey_support role.

**Fix (partial):** Handled the one shape actually reachable from the shipped UI — `DealActivityTimeline` always queries with an exact `subject_type="deal"` + `subject_id=<dealId>` filter pair for one already-open deal. When both are present and `subject_type === "deal"`, the policy layer now reuses the existing `assertLinkedDealAccess` check (the same one `portal_deal_collaborators`/`deal_line_items` already use) instead of requiring super_admin. Deliberately did **not** attempt the general case (`domain_activity_events` spans deal/task/ticket/user/... subject types with no single owning column) — the dashboard's broad, unfiltered recent-activity feed (no subject_id at all) still denies for non-super-admin and remains a real, separate open gap, same as the code's own comment already flagged.

---

### 2.23 Ch.19 19a (§19.2) — Password-reset endpoint confirms account existence and hands back a live takeover token — **fixed 2026-08-01**

**Spec (§19.2):** "Authentication errors do not reveal whether an email exists" and reset tokens must only reach the account owner through a controlled channel, stored only as hashes.

**Shipped:** requestPasswordReset (src/server/livey-service.server.ts:1844-1863) returns the raw, valid password-reset link directly in its response whenever the email matches a profile, and `{resetLink: null}` otherwise — unconditionally, in every environment, with no NODE_ENV gate. No email-sending mechanism exists anywhere in the repo (confirmed by repo-wide search: no SMTP/SendGrid/Resend/Postmark/nodemailer usage) — the reset link is never actually emailed. The public ForgotForm (src/routes/auth.tsx:357-381) then copies that live token to the caller's clipboard and shows "Reset link generated and copied to clipboard." when the account exists vs. "If that account exists, a reset link is on its way." when it doesn't (lines 373-380). Net effect: any unauthenticated visitor who knows or guesses a target's email gets both (a) confirmation the account exists and (b) a fully valid password-reset token for that account, with no need to access the victim's inbox — a direct account-takeover path, not just an enumeration side-channel.

**Fix:** identical underlying function and fix as §2.3 above — `requestPasswordReset` now always returns `{ ok: true }`, never the token/link, regardless of whether the account exists; `ForgotForm` shows the same message either way. Verified end-to-end in a browser: a real seeded email and a fabricated one produced byte-identical `{"ok":true}` responses and toast text; the real token appeared only in server-side logs. Real email delivery (to retire the log-relay stopgap) is still open — tracked in Phase 5 of §20's phased plan.

---

### 2.24 Ch.19 19b (§19.2) — Flat 14-day session lifetime for every role; a real session-revocation function exists but nothing calls it — **revocation wiring + role-tiered absolute expiry fixed 2026-08-01, idle timeout still open**

**Spec (§19.2):** Idle/absolute session lifetimes must be 30 min/12 h for Super Admin, Partner Admin, and every LIVEY-internal role, and 60 min/24 h for Partner User; password reset, MFA reset, role/scope change, suspension, offboarding, and suspected compromise must revoke affected sessions; users can view/revoke active sessions.

**Shipped:** sessionExpiresAt() (src/server/livey-service.server.ts:649-650, 965-967) issues one flat SESSION_DAYS=14 (336h) absolute expiry for every role via signInWithPassword, signUpLocal, completePasswordReset, and createSessionForUser — no idle timeout exists at all (last_seen_at is updated on every request at line 1196 but never compared against a threshold), and no role-based tiering exists anywhere. revokeUserSessionsAndContexts (lines 1363-1392) is the only function that revokes rows in `sessions`, but it has zero call sites anywhere else in src/ — completePasswordReset (1867-1937), issueTemporaryPasswordForUser (admin-forced reset, 1939-1957), and changeUserRole (identity.change_user_role, src/server/user-role-commands.server.ts:55-160, the exact function fixed for authorization this session) never call it, so a stolen or otherwise-live session token for a demoted, reset, or role-changed user (including super_admin) survives all of these events for up to 14 days. assignGovernedRole (src/server/role-assignment-commands.server.ts:215-219) revokes `active_contexts` on reassignment but likewise never touches `sessions`. No route or UI exists to list or revoke a user's own active sessions.

**Fix (partial, 2026-08-01):** `revokeUserSessionsAndContexts` is now called from `completePasswordReset`, `issueTemporaryPasswordForUser` (admin-forced reset), `changeUserRole`, and `assignGovernedRole` (which previously only revoked `active_contexts`, not `sessions`, on reassignment). Verified with a real production-build session row: `expires_at - created_at` measured exactly 12h for a super_admin login. `sessionExpiresAt()` now takes the caller's roles and returns 12h for Super Admin/Partner Admin/every LIVEY-internal role, 24h only when the caller's sole role is `partner_user` — replacing the flat 14-day lifetime. **Still open:** idle-timeout enforcement (30/60 min) — `last_seen_at` is tracked but nothing compares it to a threshold yet, since that requires touching the per-request session-validation path (`getSessionFromToken`), deliberately deferred to keep the 2026-08-01 batch's blast radius contained; and no route/UI exists yet to list or revoke a user's own active sessions.

---
## 3. Chapter 4 — Experience and interaction system

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 4a | §4.3: Desktop sticky header must contain: sidebar toggle; breadcrumb or concise current-page label; active-context switcher; assistant launcher; notification indicator; help entry; user/avatar menu. | src/components/app-shell.tsx:108-203 header has a sidebar toggle (SidebarTrigger), an 'Active context' badge block, RegionFilterSelect (src/components/region-filter-select.tsx:1-33, a plain Sales-Region data filter used to scope dashboards, not an Assignment/Working-Scope switcher), AssistantPanel, a notification Bell, and an avatar dropdown menu. There is no breadcrumb or page-title element, no help entry anywhere in the codebase (repo-wide grep for Help/HelpCircle/'/help' in components and routes returns zero hits), and no 'Switch Assignment' / 'View Within' control exists at all — src/hooks/use-auth.tsx:78 silently picks `typedAssignments[0]` (most recent row) with zero user-facing choice, matching the complete absence of any 'Switch Assignment'/'View Within'/'Start in' string anywhere in src/. | S2 |
| 4b | §4.6: All free-text search inputs are removed; lists are filtered via structured multi-select/date/numeric filters, saved views, chips, etc. | src/routes/_authenticated/rewards.tsx:373-376 and src/routes/_authenticated/support.tsx:394-397 still render a literal free-text `<Input placeholder="Search rewards"/"Search tickets">` bound to `query`/`setQuery`, substring-matched client-side (support.tsx:132-141 does `[subject, description, status, priority].join(' ').includes(term)`). support.tsx has no structured status/priority filter at all as a fallback. Other list pages (deals.tsx, customers.tsx, pipeline.tsx) were already fixed this session to use structured LookupCombobox filters instead — these two pages were missed. | S3 |
| 4c | §4.6: Lists require saved personal views, shared governed views for Super Admin/scoped roles, visible filter chips with one action to clear filters, and column visibility/ordering controls. | None of these exist anywhere in the codebase: repo-wide search for saved_views/SavedView/savedViews, ColumnVisibility/columnVisibility/toggleColumn/visibleColumns, and 'Clear filters'/clearAllFilters all return zero hits. Every list page (deals.tsx:1678-1704, customers.tsx, rewards.tsx, support.tsx, etc.) exposes only a handful of ad hoc single-select LookupCombobox filters with no persistence, no shared-view concept, no multi-select, and no chip-based clear-all affordance. | S2 |
| 4d | §4.6: "The assistant converts a conversational request into visible structured filters. Users can inspect and adjust the resulting filter state" (e.g. 'Show open India deals in Proposal owned by Partner A'). | src/components/assistant-panel.tsx's own welcome message (lines 27-31) states the scope explicitly: "I can create a new deal from what you tell me, or answer questions about your existing deals — that's all I do here." The `send()` handler (lines 44-82) only calls `sendAssistantMessage` and renders the reply plus an inline chat list of deal cards (lines 143-162); it never navigates to a list route or pushes filter state into a page's URL/filter controls. src/server/assistant.server.ts:139-151's `fetchScopedDeals` is used only to build the in-chat summary, not to drive the deals page's visible filters. The specific 'conversational request → visible structured filters the user can inspect/adjust' contract does not exist. | S2 |
| 4e | §4.5 / §5.1: "Fixed reference fields cannot create arbitrary values" and "Country and subdivision records use stable codes; users cannot create them from operational forms." | A governed 'governance.province_state_code' bucket with 11 stable codes exists in src/domain/contracts/reference-data.ts:135-149, and `isGovernedLookupField` (same file, ~line 380) correctly blocks `upsertLookupValue` for governed field names (src/server/lookup-values.server.ts ~line 55). But none of the actual UI forms use that governed field name: partner.onboarding.tsx's Region/State combobox uses `regionLookupField(form.country)` → `partners.region_prefix.<country>` (src/lib/partner-onboarding-lookups.ts), deals.tsx/admin.deals.tsx use `dealRegionLookupField(country)` → `deals.region_prefix.<country>` (src/lib/deal-lookups.ts), and customers.tsx uses `LOOKUP_FIELDS.customerRegion` = 'customers.region' (src/lib/lookup-fields.ts:16) — none of these match 'governance.province_state_code', so `isGovernedLookupField` returns false and the create path is wide open. Concretely: partner.onboarding.tsx:599-613 sets `allowCreate={false}` on Country but not on the Region/State field at lines 615-624; the same Country-locked/Region-open pattern repeats in admin.deals.tsx:578-605 and customers.tsx:847-869. Since `LookupCombobox`'s `allowCreate` defaults to `true` (src/components/lookup-combobox.tsx:88), any user filling these forms can type an arbitrary province/state value that is persisted into the shared `lookup_values` table and becomes selectable by every other user thereafter. | S3 |
| 4f | §4.5: "Closing a dialog with unsaved changes prompts the user." | No implementation exists anywhere in the codebase: repo-wide grep for beforeunload, unsaved, isDirty, hasUnsavedChanges returns zero hits in src/. Every dialog/sheet in the app (deal edit forms, customer edit forms, admin forms) closes silently regardless of pending edits. | S2 |
| 4g | §4.8: Required accessibility behaviour: "printable and downloadable certificate content that remains understandable without colour." | src/routes/_authenticated/insight-hub.tsx:126-134 `handleViewCertificate` does not render any certificate — it shows `toast.success(certificate_token, { description: "Verification reference — printable certificate view is coming soon." })`. There is no printable or downloadable certificate view anywhere in the codebase for completed Insight Hub tracks. | S2 |
| 4h | §4.4: Every operational page must handle: "Empty, loading, stale, error, offline, and permission-denied states." | Repo-wide grep for stale/Stale and offline/Offline across src/routes and src/components returns zero hits — no page implements a distinct stale-data or offline state. 'Access Denied'/permission-denied strings exist only inside server-side files (table-policy.server.ts, rbac-policy.server.ts, assistant.server.ts, dropdown-sources.server.ts) as failure-result text, not as a dedicated route-level permission-denied page state; the UI pattern actually used is a generic `toast.error(result.failure.message)` on command failure, not a first-class page state per the anatomy contract. | S3 |

---


## 4. Chapter 5 — Hierarchy, identity, context, RBAC

Beyond the S1 items in §2.1, §2.2:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 5c — **same engine fixed 2026-08-06 under 9g, see that entry** | §5.7: RM must be auto-tagged on deal creation, ISR auto-tagged and retained through Testing, and on the successful Testing → Qualified transition the engine must add the effective PAM (for the Partner) and KAM (for the Customer) in the same transaction; missing coverage must always block the transition and create a Coverage Exception rather than a partially-tagged deal. | No such engine exists. createDeal (src/server/deal-commands.server.ts:520-677) inserts only `user_id = actor` on portal_deals and never touches deal_participants. moveDealStageForward/runDealTransitionCommand (same file) contain no reference to "qualified"/PAM/KAM anywhere (grep confirms zero hits). The only way a deal_participants row is created is the fully manual tagDealParticipant/untagDealParticipant in src/server/participant-commands.server.ts:19-97, callable by any actor who passes authorizeDealActor — there is no automatic invocation of it from any transition. "Coverage Exception" (the states, SLA, and blocking behaviour in §5.7) has zero matches anywhere in src/ or db/schema.sql. Consequence: deals can move all the way through Qualified/Negotiation/Won with no PAM or KAM ever tagged, and the tag-driven visibility rows in the permission matrix (PAM/KAM/ISR = Assigned/T) never get populated by the system on their own. **Cross-reference (2026-08-06):** this is the identical gap §2.8's Chapter 9 table entry 9g describes and fixes — see that entry for the actual implementation (automatic RM/ISR tagging on creation, PAM/KAM resolution + auto-add on Testing→Qualified via `assignments`/`customer_participants`, and a new `coverage_exceptions` table that blocks the transition when either can't be resolved). Not re-implemented separately here; both entries now describe the same code. The interpretive gaps noted there apply equally here: no admin surface exists yet to actually populate a PAM assignment's `partner_id` or a Customer's KAM tag, so the Coverage Exception path is what runs in practice on every environment today. | S2 |
| 5d | §5.2/§5.3/§5.9: Assignment status must follow the exhaustive Scheduled/Active/Suspended/Ended/Revoked lifecycle with actor-constrained transitions, reasons, and session revocation; a user with several active Assignments must choose one at login and can narrow to a Working Scope or Switch Assignment/View Within in-app; offboarding must revoke sessions, place owned records in a reassignment queue, and preserve historical attribution. | The only assignment-mutating command anywhere is assignGovernedRole (src/server/role-assignment-commands.server.ts:82-310): it unconditionally sets any prior active assignment to `status='ended'` (:155-160) and inserts the new one directly as `status='active'` (:163-179) — there is no code path that ever writes `status='suspended'` (grep across src/server confirms 0 hits), no future-dated Scheduled assignment, and no distinct revoke-with-reason command constrained to a Partner Admin acting only on a subordinate assignment (canGrantRole in governance.ts:303-309 only gates role escalation, not status transitions — no transition-guard function matching the §5.2 table exists at all). use-auth.tsx (:76-79, `typedAssignments[0]`) and app-shell.tsx (:64-136) only ever display a single most-recent assignment as a read-only badge; there is no login assignment-selection UI, no Working Scope/"Start in" selector, and no Switch Assignment/View Within control — the only region-related control in the header, RegionFilterSelect (src/components/region-filter-select.tsx + src/lib/region-filter.tsx:1-77), is an unauthenticated, purely client-side localStorage filter with no server validation, no audit event, and no effect on the governed Assignment/geography ceiling. revokeUserSessionsAndContexts (src/server/livey-service.server.ts:1363-1392), the one function that correctly revokes both `sessions` and `active_contexts` together, is defined but never called from anywhere in the codebase (grep confirms only its own definition) — so §5.9's "authentication sessions are revoked" on offboarding/suspension has no invoking code path, and there is no reassignment queue, manager-succession, or open-work-reassignment mechanism anywhere. | S2 |
| 5e | §5.4 (Distributor): Distributor access must be strictly participant-based: the user sees only explicitly tagged Customers/Deals via an exact safe-field/command allowlist, plus a dedicated fulfillment state machine (Awaiting Stock → Ready to Dispatch → Dispatched → Delivered/Exception) driven by `logistics.updateDistributorFulfillment` and `document.uploadSharedFulfillment`. | The role key `restricted_distributor` exists and is wired into geography/table scoping like any other internal role, but the participant-gated safe view and the entire fulfillment command set are absent — confirmed by the codebase's own comment in src/lib/partner-status.ts:224-229: "The record-level half of the contract — seeing ONLY explicitly tagged Customers and Deals, the fulfillment-safe field projection, and the `logistics.updateDistributorFulfillment` command allowlist — is a later phase. Until it lands, the Deals/Customers surfaces a Distributor reaches are still narrowed server-side by assignment scope... not by these flags." A grep across src/ for "Awaiting Stock"/"Ready to Dispatch"/"updateDistributorFulfillment"/"uploadSharedFulfillment"/"fulfillment_state" returns zero implementation hits (the only "FULFILLMENT_STATUSES"/"FULFILLMENT_STATE_MACHINE" found in taxonomy.ts:184-192 and state-machine.ts:441-443 is an unrelated reward-fulfillment enum: queued/in_progress/fulfilled/failed/canceled, not the Distributor logistics states). Per finding 5b, a Distributor's actual deal/customer visibility today collapses to self-created-only rather than either the intended "explicitly tagged" model or even the geography-ceiling fallback the comment describes. | S2 |

---


## 5. Chapter 6 — Authentication, onboarding, profiles, teams

Beyond the S1 items in §2.3:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 6b | §6.3: The partner lifecycle must include Suspended and Offboarded as first-class states with defined transitions/side-effects (revoke sessions, retention-governed access), and 'all unlisted transition pairs and direct status-field updates are rejected.' | PARTNER_LIFECYCLE_STATUSES (src/domain/contracts/taxonomy.ts:49-59) has only 9 states and lacks 'suspended'/'offboarded' entirely; no UI or server path anywhere suspends or offboards an Approved partner (repo-wide grep for suspend/offboard partner logic returns nothing outside an unrelated assignment-status comment). The canonical PARTNER_STATE_MACHINE (src/domain/contracts/state-machine.ts:159-187) is dead code — grep shows it is referenced only by domain/contracts/contracts.test.ts, never by any real command. The actual write path, admin.partners.tsx's decide() (lines 293-389) and approveSignedAgreement() (lines 462-542), sets partners.status/profiles.partner_status directly via generic table updates with only an ad hoc client-side check for the 'approved' decision — no server-side transition guard exists for any other decision. | S2 |
| 6c | §6.1/§6.7: Identities created by an authorised Super Admin/Partner Admin inviter must receive an activation link or temporary credential that forces a password change; credential status must be tracked (Invited, Delivered, Opened, Activated, Expired, Revoked); approval must never silently create a reusable password. | Both admin-created users and partner-invited teammates have the inviter type the new user's initial password directly in a plaintext form field ('Set an initial password', src/routes/_authenticated/admin.users.tsx:655-661; password field in partner.team.tsx:499-505/addMember lines 155-220), and insertWorkspaceUserRecord (src/server/livey-service.server.ts:1674-1687) defaults must_reset_password to false on both paths, so the new user is never forced to change the password the inviter chose for them. portal_team_members.status (src/lib/portal-records.ts:195-210) is a bare string limited to invited/active/paused, with no Delivered/Opened/Activated/Expired/Revoked lifecycle. Only the separate issueTemporaryPasswordForUser path (used solely by 'Grant final approval' in admin.partners.tsx) actually generates a real temporary password and forces a reset. | S2 |
| 6d | §6.9: Partner Admin invitation requires Region/Country/Province assignment, start date, optional end date, manager, a data/module permission preview, and an invitation message, in addition to name/email/phone/role/title/responsibility. | partner.team.tsx's TeamForm (lines 45-65) and its rendered fields (lines 486-551) only capture full_name, email, password, role_title, portal_role, responsibility, phone, and status — no geography assignment, manager, start/end date, permission preview, or invitation message exist anywhere in the invite form or createPartnerTeamMembersBulk's input shape. assignGovernedRole, the only function that writes a real `assignments` row with a geography ceiling (src/server/role-assignment-commands.server.ts:82-164), is called only from admin.users.tsx (per its own comment at line 325) — team-invited members never receive a governed assignment/geography scope at all, just a portal_team_members roster row with a hardcoded permissions array. | S2 |
| 6e | §6.4/§6.5: The wizard's first step must capture Partner Admin identity (role in company, phone, authorised-signatory indicator); Years in Business and Business Focus are two of the five dictionaries that Section 23.3 alone governs, and onboarding controls 'never carry a second hard-coded list.' | partner.onboarding.tsx has no 'Partner Admin identity' step at all — repo-wide grep for 'signatory' or 'role in company' returns zero hits, and the wizard's first step (business) only captures company legal identifiers (GST/PAN/CIN/website). years_in_business is a free-typed integer field (companySchema, lines 80-88: `z.coerce.number().int()`, rendered as `<Input type="number">` at line ~638), not a governed band. business_focus is rendered from a hardcoded local FOCUS_AREAS array (lines 56-67), and lib/partner-onboarding-lookups.ts (lines 1-9) has no businessFocus lookup field defined at all, confirming it never reads the Section 23.3 registry the way business_type/annual_turnover/employee_count correctly do via LookupCombobox. | S2 |
| 6f | §6.4/§6.10: Changes Requested must deep-link to the specific affected fields and preserve reviewer notes; a Changes Requested user can edit only the requested or dependent sections, not the whole application. | partner.onboarding.tsx's `readOnly` boolean (lines 153-160) explicitly excludes the 'need_more_info' status, so when a reviewer requests changes the ENTIRE wizard reopens for editing, not just the flagged fields. admin.partners.tsx's decide('need_more_info') path (lines 293-332) only stores one free-text note in partner_review_notes — there is no data structure anywhere identifying which specific fields/documents require correction, so the client has nothing to deep-link to even if it wanted to. | S2 |
| 6g | §6.6: Partner Approvals must provide duplicate company/domain/tax-ID/relationship-assignment warnings, country/region and PAM/RM assignment recommendations, SLA cards, and must keep internal notes and partner-visible requests separate. | admin.partners.tsx has none of the above: no duplicate-detection logic, no PAM/RM recommendation UI, no SLA/queue-age indicators anywhere in the file (grep for duplicate/recommend/SLA returns nothing). Worse, the single free-text `noteDraft` a reviewer types is used both as the internal `partner_review_notes` record AND, verbatim, as the message sent to the partner via recordNotification (decide(), lines 326-340) — internal and partner-visible communication are the same text, not kept separate as required. | S2 |
| 6h | §6.1: The sign-in sequence requires a security-challenge (MFA) step when enabled, rate limiting by account/IP risk/device signal, and security-event creation for new devices or suspicious sign-ins. | signInWithPassword (src/server/livey-service.server.ts:1449-1502) is a plain email/password check with no throttling, no MFA/challenge step, and no device/security-event recording of any kind. A repo-wide grep for MFA/rate-limit/security-event/new-device/suspicious-sign-in/TOTP/OTP concepts turns up nothing relevant (only an unrelated shadcn `input-otp` UI primitive and a telemetry log-redaction key list). Login is entirely unthrottled password guessing with no risk signal captured anywhere. | S2 |

---


## 6. Chapter 7 — Navigation and role dashboards

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 7.3-7.9 | §7.3-§7.9: Super Admin, RM, PAM, KAM, ISR, and LIVEY Support each require a distinct dashboard with named, role-specific cards/panels (e.g. RM: regional pipeline + deals missing required PAM/KAM/ISR/Distributor decision + team load/offboarding queue; PAM: assigned partners + discount/won reviews + distributor-tagging prompts; KAM: account health + renewals + deals added after Testing; ISR: sourced/demo/testing funnel + conversion rate; LIVEY Support: ticket queues by status/SLA/product/severity + Quick Actions for assignment/escalation/reopen). | src/routes/_authenticated/dashboard.tsx is a single shared component for all 9 roles. Its only role branching is `roles.includes("super_admin") ? "Super Admin" : roles.includes("partner_admin") ? "Partner Admin" : "Partner User"` (lines 98-102) and a few `hasRole("super_admin")`/`hasRole("partner_admin")` checks for a News-publish button and onboarding checklist (lines 435-441, 657-662, 677-710). Grepping the whole file for "rm"/"pam"/"kam"/"isr"/"livey_support"/"restricted_distributor" returns zero matches. Every LIVEY-internal role that isn't super_admin gets the exact same generic 5-KPI set (pipeline/open deals/approved partners/customers/reward points, lines 322-358) built from the same queries — no registration/won-review queue, no reward-liability/redemption-exception cards, no integration-health panel, no ticket-SLA panel, no account-health/renewal cards, no sourced-lead funnel, anywhere in the codebase. | S2 |
| 7.2 | §7.2: Every dashboard must include a My Tasks/overdue-work panel, a primary work queue, relevant approvals/SLA alerts, structured drill-down filters, an active-context switcher (not just a heading), and a data-freshness indicator. | None of these exist on the shared dashboard. Quick Actions (dashboard.tsx lines 642-666) link to /deals, /pipeline, /customers, /analytics, /rewards, /deal-documents, /partner/onboarding, /admin/news, /settings — there is no /tasks entry and no My Tasks widget anywhere in loadDashboard's queries (lines 112-407, no `tasks` table query at all). There is no approvals/SLA-alert card and no drill-down filter beyond the global region selector (`useRegionFilter`). The header's "active-context" element (app-shell.tsx lines 108-147, buildShellContextSummary in app-shell.utils.ts) only displays the single current assignment/context as read-only badges plus a "Refresh context" button that re-fetches the same context (`void refresh()`, line 142) — there is no function in src/hooks/use-auth.tsx to switch to a different assignment/context (no setActiveContext exposed, no switch handler exists anywhere in the repo). The "source" badge (lines 430-434) only says "Live Postgres data" vs "Empty state", not a freshness timestamp. | S2 |
| 7.1 | §7.1 (last paragraph): "The Distributor view excludes broad Administration, internal Partner lists, global analytics, pricing policy, strategic audit, and untagged records." | The access-flags layer correctly models this: `canAccessAnalytics`, `canAccessPartnerDirectory`, `canAccessPricingPolicy`, `canAccessStrategicAudit` are computed per role in src/lib/partner-status.ts and set to `false` specifically for `restricted_distributor` (lines 240, 265-268) with an explicit comment citing §7.1. But grepping the entire src tree shows these four flags have zero consumers anywhere outside their own definitions in partner-status.ts — app-sidebar.tsx never reads them. The sidebar's "Workspace" group (Deals/Pipeline/Tasks/Customers/Analytics/Insight Hub, lines 47-54) has no `roles` gating on any item and renders in full whenever `canAccessDeals` is true (line 81, 183) — true for Distributor — so the Analytics link is shown to a Distributor. Separately, src/routes/_authenticated/analytics.tsx guards only with `useRequireAccess("full")` (line 39), which checks the coarse `accessLevel === "full"` (src/hooks/use-partner-access.ts / getPartnerAccessFlags), and `distributorFlags` hardcodes `accessLevel: "full"` (partner-status.ts line 234) — so the guard passes for a Distributor regardless of `canAccessAnalytics: false`. A Distributor can therefore reach and export (XLSX download button in analytics.tsx) the Analytics page the code was explicitly built to deny them; actual data exposure is narrowed only incidentally by applyPartnerScope's user_id fallback, not by any enforcement of the named exclusion. | S2 |
| 7.2/7.12 | §7.2 / §7.12: "Clicking a KPI opens its underlying scoped records with visible filters applied" and "KPI cards are actionable and preserve filter state." | getDashboardMetricDestination (src/lib/global-search.ts lines 82-97) maps each KPI id to a bare static route with no query/filter state: `pipeline` → "/pipeline", `deals` → "/deals", `partners` → "/admin/partners" or "/partner", `customers` → "/customers", `rewards` → "/rewards". Clicking "Open deals" (a count of non-won/lost deals) lands on the unfiltered /deals list, not a stage-filtered view; clicking "Approved partners" lands on the unfiltered partner admin list, not one pre-filtered to status=approved. No filter parameters are passed or read on any destination route. | S3 |

---


## 7. Chapter 8 — Partners, Customers, Contacts

Beyond the S1 items in §2.4:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 8.7b | §8.7: Two named Customer participant commands (Add/Remove Distributor to/from Customer) and two named Deal participant commands (dealParticipant.addDistributor/removeDistributor) must each: select an active Distributor Assignment, preview the safe field set / Section 5.4 allowlist, take reason+effective dates, create the participant period + Activity + in-app notification atomically, and (on removal) preview/transfer open shared Tasks or shipment responsibility before ending the tag. | No differentiated distributor command exists anywhere; only a generic, asymmetric participant-tag mechanism is shipped. Customer side (src/routes/_authenticated/customers.tsx saveParticipant, lines 465-506, and the participant_type LookupCombobox options at lines 1216-1223) supports ADD only - the options list is ['primary_owner','collaborator','approver','observer','support_contact'], there is no 'distributor' option, no Distributor Assignment picker, and no remove/end-tag UI at all (grepped the file: no delete/valid_to-setting call exists outside the read-only display). Deal side is the mirror-image gap: src/components/deal-participant-tags.tsx only wires up untagDealParticipant (lines 46-57); the corresponding tagDealParticipant/'add' command (src/server/participant-commands.server.ts:19-59, exposed via src/integrations/local/participant-commands.ts:41-43) has a working server implementation but is never imported or called from any route or component in the repo (grep across src turned up zero UI callers) - a Deal participant can never be added through the product. Neither command validates that the participant_id/participantUserId corresponds to an actual active Distributor Assignment, and neither creates the required Activity entry or in-app notification atomically with the tag change. | S2 |
| 8.1/8.2 | §8.1/§8.2: Partner detail page must expose summary/Company/Team/Coverage/Agreements & Documents/Deals/Customers/Learning/Rewards/Support/Activity/Audit tabs (authorised tabs only); Super Admin needs Add Partner + import actions; operational lists need status/tier/region/country/coverage/PAM/RM/certification/date filters and clickable stat cards; bulk actions need scope+impact-preview+result-report. | No consolidated Partner detail page with this tab structure exists anywhere in the repo (searched all routes under src/routes for a partner-detail component). What exists instead: src/routes/_authenticated/admin.partners.tsx is only an approval-queue Sheet with Business Information / Company Details / Business Focus / Documents / Review notes sections and status-transition buttons (approve/reject/request-info) - no Team, Coverage, Deals, Customers, Learning, Rewards, Support, Activity, or Audit tab is present. Its list view (lines 554-666) filters only by a single Status LookupCombobox plus a company-name/GST text search - no tier, region, country, coverage, PAM, RM, certification, or date filters, and no clickable stat cards. There is no 'Add Partner' button or partner-import action anywhere (grepped admin.partners.tsx/admin.users.tsx for 'Add Partner'/'import' - zero hits; partners are only created via partner self-registration), and no bulk-action UI at all. src/routes/_authenticated/partner.tsx is a separate partner-self-service 'Company profile' page (not an admin-facing detail view) that also lacks the tab structure. | S2 |
| 8.4 | §8.4: A Contact/POC entity with full name, company association, title/role, work email, phone, WhatsApp eligibility, country/timezone, communication consent, preferred channel, active/inactive status, and Activity must exist; other records must link to a Contact ID rather than copy a free-form POC name, using the standard POC Name/Job Title/Email/Phone/Preferred Channel field set. | No Contact/POC entity exists anywhere in the codebase. db/schema.sql has no contacts table (grepped all CREATE TABLE statements). No file anywhere defines POC job title, work email, WhatsApp eligibility, preferred channel, or communication consent fields (grepped repo-wide for whatsapp/preferred_channel/communication_consent/poc_job_title/poc_email/poc_phone - only an unrelated 'WhatsApp Business' integration toggle string matched). Deals still store a free-text contact_name column (db/schema.sql:328) tied only to customer_id, not to any person-level Contact ID; the deal's separate poc_profile_id field (db/schema.sql:1032) resolves to the internal LIVEY owner/profile via a 'POC' LookupCombobox (src/routes/_authenticated/deals.tsx:1858-1877), which is a different concept (internal deal owner) and not a substitute for a customer-side Contact/POC record. This entire sub-section is unbuilt, not merely divergent. | S2 |
| 8.5 | §8.5: Saving a next step must: timestamp the Activity, optionally create a Task, identify an assignee, record a due date/time and timezone, and notify the event's eligible typed participants (Section 5.8 recipient algorithm) - while remaining visible in the timeline after completion. | saveActivity() in src/routes/_authenticated/customers.tsx (lines 423-463) only inserts a summary/next_step free-text pair into portal_customer_activities and copies the same next_step string onto portal_customers.next_step - there is no Task-creation call (no reference to a tasks table or task-commands.server.ts anywhere in this file), no assignee selector in the Activity/Next-step form fields (lines 1092-1098 and 926-931 are plain Textareas), no due-date/time/timezone input anywhere in the dialog, and no call to recordNotification or any notification helper (grepped the whole file plus src/lib/workflow-events.ts for a call site - none exists in customers.tsx). Four of the five required behaviors from this sub-section are absent; only the bare append-only timestamp+summary survives. | S2 |
| 8.6 | §8.6: "Auto CRM and manual creation run duplicate detection ... Potential duplicates are held for review" before the record is usable. | Duplicate detection (detectCustomerDuplicateGroups/detectCustomerDuplicateMatches in src/lib/customer-governance.ts) is real and correctly implemented, but it is only ever invoked client-side, informationally, inside the customer edit dialog against the already-existing full customer list (src/routes/_authenticated/customers.tsx:257-270). Neither creation path calls it: createCustomer() in customers.tsx (lines 321-348) inserts the new row directly via supabase.from('portal_customers').insert(payload), and the server-side createCustomerFromDropdown (src/server/dropdown-sources.server.ts:379-429, used by the lookup-combobox 'create' flow everywhere) likewise inserts directly with no duplicate check. No record is ever held, flagged, or queued for review at creation time - duplicates are only surfaced after the fact, and only if a user happens to reopen the resulting record's edit dialog. | S3 |

---


## 8. Chapter 9 — Deals, pricing, approvals, pipeline

Beyond the S1 items in §2.5, §2.6, §2.7, §2.8:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 9e — **fixed 2026-08-06 (destination stage + mandatory reason only)** | §9.10/§9.19: Any active tagged participant may request and perform a permitted backward movement through a dialog requiring destination stage, mandatory reason category, explanation, task/approval treatment, and a notification preview. | moveDealStageBackward is fully implemented server-side with mandatory reason validation (src/server/deal-commands.server.ts:378-410), but grep across the whole src tree shows it is referenced only by its own integration wrapper (src/integrations/local/deal-commands.ts) and its unit test — zero callers exist in deals.tsx, pipeline.tsx, or admin.deals.tsx. There is no button, dialog, or menu item anywhere in the shipped product that lets any user move a deal backward. **Fix:** verified via grep this was still exactly true (zero UI callers) before implementing. Added a "Move backward" action + dialog in two places: `deals.tsx`'s deal-detail action row (next to Advance/Mark won/Mark lost, shown only when `getValidBackwardStages(selectedDeal.stage)` — a new shared helper in `portal-records.ts` mirroring the server's own `isBackwardMove` check exactly, including "a terminal `from` stage is never a valid backward move" — is non-empty), and `pipeline.tsx`'s board card (same dialog shape, per-card). Each dialog captures a destination stage (`LookupCombobox` restricted to valid earlier non-terminal stages) and a reason (`Textarea`, Confirm disabled until non-empty), calls `moveDealStageBackward`, then records a notification + `portal_audit_events` row on success, matching the existing pattern every other transition button in these two files already follows. **Not attempted:** the spec's fuller dialog fields (a structured "reason category" taxonomy distinct from free-text explanation, a task/approval treatment picker, and a notification preview before sending) — this pass ships the destination-stage + mandatory-reason core the server already enforces, not the full §9.10 dialog spec. | S2 |
| 9f — **partially fixed 2026-08-06 (Add-line-item control shipped; creation-flow ordering still open)** | §9.3(Section 2)/§9.4/§9.5/§9.6: Product/quantity capture happens before Quantity in every creation path, with each line carrying Product/Combo, SKU, description, quantity, UOM, catalogue price version, MSRP, Base PTP, tier adjustment, PTP, discount, DTP, and proposed selling price — the basis for the registration threshold and all pipeline-value metrics. | createDeal (src/server/deal-commands.server.ts:514-677) and its form (deals.tsx:1933-1941) capture only a single free-text `product` combobox value and one free-text `amount` string — never deal_line_items. addDealLineItem/updateDealLineItem exist server-side and are wired through src/integrations/local/pricing-commands.ts, but DealLineItems (src/components/deal-line-items.tsx) only renders a Remove button and a "Freeze Pricing" action (lines 83-133) — there is no "Add line item" control anywhere in the app, so deal_line_items can never be populated through the product. Consequently submitDealForRegistration's threshold check falls back to the free-text `amount_usd`/`amount_value` (deal-commands.server.ts:716-720) for essentially every real deal, meaning the entire catalogue-driven MSRP/Base-PTP/tier/DTP pricing engine (pricing-domain.ts) is disconnected from the deal lifecycle in practice. **Fix:** `deal-line-items.tsx` now has a real "Add line item" dialog (product/SKU picker, quantity, MSRP/PTP/discount%/DTP/proposed-selling-price fields, reward-eligible checkbox) calling `addDealLineItem` — the literal "zero add control" bug is closed, and deal_line_items can now be populated through the product for the first time. **Still open:** this is a post-creation, optional action in the deal-detail view, not part of `createDeal`'s own flow — §9.3's requirement that product/quantity capture happens as an ordered step *during* creation (before Quantity, in every creation path) is untouched; `createDeal` still takes one free-text product+amount pair, and a deal can still be fully created and submitted for registration with zero line items, still falling back to the free-text amount. Reordering deal creation itself into a line-item-first flow is a larger, separate redesign, not attempted here. | S2 |
| 9g — **fixed 2026-08-06 (a/b/c below); interpretive gaps noted** | §9.3(Section 4)/§9.11: RM is tagged automatically on deal creation, ISR automatically or by routing; the Testing→Qualified transition must auto-add the Partner's PAM and Customer's KAM, and a missing mapping must create a Coverage Exception that blocks the transition. | tagDealParticipant (src/server/participant-commands.server.ts:19-59) is never invoked by createDeal, moveDealStageForward, or any other deal-lifecycle command — grep for its call sites in src/server finds only its own definition. DealParticipantTags (src/components/deal-participant-tags.tsx) wires only untagDealParticipant (line 4, 49) — there is no "add participant" UI anywhere. A grep for "Coverage Exception"/"handoff" across the whole src tree returns zero matches. Net effect: deal_participants rows can only ever be viewed or removed through the shipped product, never created — no automatic RM/ISR tagging exists, and the entire §9.11 post-Testing handoff (PAM/KAM auto-add plus Coverage Exception gating) is unbuilt. **Fix (a) — automatic RM/ISR tagging on creation:** `createDeal` (deal-commands.server.ts) now inserts a `deal_participants` row (source='automatic') for the creating actor when their `roleKey` is `rm` or `isr` — the only context this command has to determine either without a real routing/region-assignment engine, which doesn't exist in this codebase. A deal created by any other role gets no automatic RM/ISR tag. **Interpretive gap:** §9.3's "RM is tagged on all deals in the RM's effective regional scope" implies resolving the covering RM even when a *different* role creates the deal (e.g. partner_admin) — that needs a real region-to-RM assignment-resolution engine this codebase doesn't have; not attempted. **Fix (b) — Testing→Qualified PAM/KAM auto-add:** `moveDealStageForward`'s `resolveTarget` is now async and receives the transaction client, so the Testing→Qualified transition specifically calls a new `resolveTestingToQualifiedHandoff`: it resolves the Partner's PAM via `assignments` (`role_key='pam' AND partner_id = <deal's partner>`, reusing the exact column partner_admin/partner_user/restricted_distributor assignments already use for tenant scoping) and the Customer's KAM via `customer_participants` (`participant_type='KAM'` on the Customer record, reusing the same tag mechanism Distributor-to-Customer tagging already uses, since `assignments` has no customer-scoped column at all). Both are resolved *before* either is written, so a deal is never partially tagged (writing one the moment it resolves would have tagged e.g. KAM while PAM was still missing — a first version of this did exactly that and a test caught it; see Verification). **Interpretive gap, stated plainly:** no admin UI in this codebase ever sets a PAM assignment's `partner_id` or tags a Customer's KAM via `customer_participants`, so both resolutions return nothing on every real environment today — meaning the Coverage Exception path below is what actually runs for every real Testing→Qualified attempt right now, which is the honest, spec-correct behavior for "no mapping exists," not a bug. Building the admin surface to actually create those mappings is a separate, larger piece of work, not attempted. **Fix (c) — Coverage Exception schema + block:** added a new, minimal `coverage_exceptions` table (`db/schema.sql`, plain `CREATE TABLE IF NOT EXISTS` — a genuinely new table, not an existing one needing an ALTER) with `required_role`/`coverage_key`/`deal_id`/`action`/`status`/`detected_at`/`sla_target_at`/`responsible_role`/`resolution`/`resolved_at`. When PAM and/or KAM can't be resolved, one `coverage_exceptions` row (status='open') is inserted per missing role and the transition returns a validation failure — the stage UPDATE never runs, so Testing→Qualified is fully blocked, matching "the transition does not commit." **Not built (documented per the task's own explicit deprioritization if time-constrained):** the full Open → In Remediation → Resolved/Cancelled state machine, any queue/remediation UI, SLA countdown, and reconciliation-retry — `status`/`resolution`/`resolved_at` columns exist for a future remediation surface to write to, but nothing reads or transitions them yet; every exception this code creates stays 'open' forever until a human fixes it directly in the database or a future session builds that surface. **Verification:** `tsc --noEmit`, `eslint` clean on every touched file (`deal-commands.server.ts`, `deal-commands.server.test.ts`, `db/schema.sql`). 6 new tests in `deal-commands.server.test.ts` via a new `installFakePoolForHandoff` harness (discriminates the assignments/customer_participants/deal_participants/coverage_exceptions queries by SQL text): both PAM and KAM resolve → both participants added, stage commits, zero coverage exceptions; PAM missing → transition blocked, zero deal_participants writes (including for the KAM that *did* resolve — this is the test that caught the partial-tag bug above), one coverage_exceptions row with `required_role='pam'`; KAM missing → symmetric; plus 3 createDeal tests (rm creator tags RM, isr creator tags ISR, super_admin creator tags neither). Full deal-commands suite: 40/40 pass. `bun run build` clean. Not browser-verified — there is no seed data anywhere with a PAM assignment's `partner_id` set or a KAM `customer_participants` tag, so this couldn't be exercised end-to-end against real data without first building the (out-of-scope) admin surface to create such a mapping. | S2 |
| 9h — **fixed 2026-08-06 (minimal UI); approval snapshotting still open** | §9.12: At Proposal, authorised Partner users can request a discount; tagged PAM/RM or Super Admin approves, changes, or rejects it, with the approval snapshotting approver/policy version/percentage/DTP/reason. | requestDiscount and approveDiscount exist server-side (src/server/pricing-commands.server.ts:292-420, approveDiscount's authorizeDealActor gap already fixed this session) but a repo-wide grep for both names against every .tsx file returns no matches — there is no discount-request form, no approval queue, and no discount indicator anywhere in deals.tsx, pipeline.tsx, or admin.deals.tsx. The entire §9.12 discount workflow is unreachable from the shipped UI. **Fix:** added a minimal but real UI to `deal-line-items.tsx` (the natural home now that 9f gave it real line items to attach a discount to — `discount_requests.line_item_id` is a required FK, so a discount request always needs an existing line item). A "Request discount" icon button per line item (visible at Proposal or Negotiation, matching `requestDiscount`'s own stage gate exactly) opens a dialog capturing percentage + reason and calls `requestDiscount`. A "Discount requests" panel below the table lists every request for the deal (product, %, reason, status badge); when the viewer's `roleKey` is `pam`/`rm`/`super_admin` (mirroring `approveDiscount`'s own role check verbatim) and a request is `pending`, Approve/Reject buttons call `approveDiscount`. Exposed `requestDiscount`/`approveDiscount` via thin wrappers in `src/integrations/local/pricing-commands.ts`, matching every other command in that file. `discount_requests` was not previously reachable through the generic table-policy read path at all (absent from `TABLE_FEATURE_MAP`/`TABLE_COLUMNS`/`getScopeSpec`) — registered it exactly like `deal_line_items`/`portal_deal_collaborators` (feature `"deals"`, `{kind: "linked-deal"}` scope, read-only column allowlist); writes still exclusively go through the two named commands, never this generic path. **Still open:** the approval snapshot the spec requires (approver/policy version/percentage/DTP/reason frozen at approval time) — `approveDiscount` records `approver_id`/`status` but not a policy-version snapshot; that's a deeper server-side change to the command itself, not attempted here (out of this UI pass's scope). | S2 |

---


## 9. Chapter 10 — Tasks

Beyond the S1 items in §2.9, §2.10:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 10c | §10.1 / §10.7: Every Task must carry: human-readable Task ID, structured type, watchers, active context/scope, several related records (without duplicating the Task — an explicit §10.7 acceptance criterion), start date, due date/timezone, recurrence rule, dependencies, checklist, blocking-stage indicator, completion evidence, automation source/template, and separate internal vs partner-visible descriptions (§10.5). | The tasks table (db/schema.sql:1325-1344) has only: title, description (single, shared field — no internal/partner-visible split), status, priority, one related_type/related_id pair, assignee_id, creator_id, partner_id, due_at, blocked_reason (free text, not a structured dependency), completed_at, cancelled_at, version. There is no task_related_records, task_dependencies, task_checklist, task_watchers, task_templates, or recurrence table/column anywhere in db/schema.sql (grep for all of these returns nothing). A Task can relate to exactly one record, not several, directly contradicting the §10.7 criterion; there is no template/automation-source linkage despite §10.7 requiring "a generated Task records its template and automation source." | S2 |
| 10d | §10.4: Stage templates must create required/optional Tasks, assign by participant role, calculate due dates from stage entry, require evidence, block transitions while incomplete, and apply a documented cancel/retain/reopen treatment on backward movement, per stage (Sourced, Demo, Testing, Qualified, Proposal, Negotiation, Won stages, Lost). | createTask (src/server/task-commands.server.ts:230-294) is called from exactly three places in the whole repo: tasks.tsx's manual "New task" dialog, its own test file, and the thin server-fn wrapper — never from deal-commands.server.ts or any other stage-transition code (grep for "tasks"/"createTask" in deal-commands.server.ts returns zero hits). No stage template, due-date-from-entry calculation, evidence requirement, transition-blocking check, or backward-movement Task treatment exists anywhere; the deal pipeline and the Task module are entirely disconnected. | S2 |
| 10e | §10.3 / §10.5 / §10.6: Task workspace needs Today/Upcoming/Overdue/Assigned-by-Me/saved views and list/grouped/calendar/board presentations with structured filters; assignment must be validated against the assignee's access to related records and rejected outside scope; reminders/escalation (in-app, email/WhatsApp digest, non-dismissible SLA alerts, deduplication, offboarding reassignment) must exist. | tasks.tsx (src/routes/_authenticated/tasks.tsx) renders only a flat list with status tabs (all/to_do/in_progress/blocked/completed/cancelled, lines 365-374) — no Today/Upcoming/Overdue/Assigned-by-Me tabs, no saved views, no filters beyond status, no calendar/board/grouped presentation, and no assignee picker in the create dialog (creation always self-assigns: `assigneeId: profile?.id ?? null` at line 122). createTask (src/server/task-commands.server.ts:230-294) accepts any `assigneeId` from the caller with zero check that the assignee has access to the related record or is even in the same partner/tenant scope — the §10.7 criterion "Assignees cannot be selected outside scope" is unenforced at the command layer. Separately, no reminder or escalation code exists for tasks anywhere in src/server or src/routes (grep for reminder/escalat across both trees returns no task-related files). | S2 |

---


## 10. Chapter 11 — News, Activity, Notifications

Beyond the S1 items in §2.11, §2.12:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 11c | §11.2: A News post must carry publication status (Draft/Scheduled/Published/Expired/Archived), publish/expiry timestamps, pinned/priority state, an audience expression (Global/Region/Country/Partner/tier/role/etc.), digest behaviour, acknowledgement requirement, revisions, analytics, and a lifecycle/approval workflow for high-priority global posts. | portal_news_posts (db/schema.sql:657-668) only has title, caption, image_path/image_alt, posted_by_name/role, created_at/updated_at, is_seed — no status column, no scheduling/expiry, no priority, no audience column, no digest flag, no acknowledgement, no revisions table, no analytics. admin.news.tsx (the only News authoring surface) implements just create/update/delete of those fields (createPost src/routes/_authenticated/admin.news.tsx:145-174, savePost:176-201, deletePost:203-220) with no state machine, no scheduler, no audience picker, and editing overwrites the row in place (no version history is retained). news-feed-card.tsx (the only render surface) has no acknowledgement control, no unread indicator, no deep link. Every post is immediately visible to literally every authenticated user (see 11b) — there is no audience-dimension concept at all, let alone the combinable Global/Region/Country/Partner/tier/role targeting the blueprint specifies. | S2 |
| 11d | §11.7/§11.8: Email and WhatsApp digest delivery must exist with user-configurable schedule/timezone/categories/quiet-days/language, and delivery must track a full state machine (Pending/Sent/Delivered/Read/Failed Retryable/Failed Permanent/Suppressed by Preference/Suppressed by Access/Cancelled) with signature-validated, deduplicated provider callbacks. | No email or WhatsApp transport exists anywhere in the repo: `grep -rn "nodemailer\|resend\|sendgrid\|smtp\|twilio" package.json src/` returns nothing. The only WhatsApp/email-related UI is admin.integrations.tsx's INITIAL_PROVIDERS array (src/routes/_authenticated/admin.integrations.tsx:54-107), a hardcoded mock list ("WhatsApp Business"/"Postmark Email" cards with fabricated "lastOutbound: 2 mins ago" strings) with no backing service. There is no delivery/digest table in db/schema.sql (grep for "delivery\|digest\|quiet_day\|unsubscribe" returns nothing) and no notification-preference table or UI (grep for "preference"/"digest" across src/ turns up only unrelated crypto `.digest("hex")` calls). This entire capability is absent, not a stub. | S2 |

---


## 11. Chapter 12 — Assistant, website/WhatsApp, Auto CRM

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 12a | §12.5/§12.6/§12.7-§12.11: A public website business assistant (FAQs, product discovery, lead qualification, contact capture/consent, meeting/callback request, ticket guidance, human handoff) and a WhatsApp business workflow (approved-template FAQs, lead capture, Deal draft capture for identified Partners, ticket status flows, digest delivery, human handoff, phone-to-Contact verification) must feed a full Auto CRM Lead lifecycle: 10 named Lead states (New, Needs Verification, Duplicate Review, Qualified, Nurturing, Assigned, Claimed, Converted, Disqualified, Closed) with a detailed guarded transition table, deterministic/fuzzy deduplication, enrichment with source/confidence, explainable scoring, geography/Partner/ISR/product/workload routing, and a structured conversion preview before creating/linking a Customer/Contact/Deal Draft. | None of this exists. There is no `leads`/`portal_leads` table anywhere in db/schema.sql (grep for "lead" returns zero matches), no lead server-commands file, and no lead UI route (`find src/routes -iname "*lead*"` returns nothing). The only lead-shaped code is an orphaned stub: `LEAD_STATUSES` in src/domain/contracts/taxonomy.ts:134-142 defines just 5 generic states (new/contacted/qualified/converted/disqualified — none of the blueprint's 10 states), and `LEAD_STATE_MACHINE` in src/domain/contracts/state-machine.ts:320-338 wires a trivial transition graph (`new: ["contacted","qualified","disqualified"]`, etc.) with no guard/actor/evidence/idempotency modeling matching §12.7's table. `grep -rn "LEAD_STATE_MACHINE" src/` (excluding its own definition file) returns nothing — it is never imported or invoked by any server command, route, or UI component. There is no website-facing assistant, no WhatsApp inbound webhook or outbound message handler anywhere in src/ (`grep -rn -i "whatsapp\|twilio" src/` besides a UI label), and no human-handoff mechanism at all (`grep -rn "handoff" src/` is empty — no Task/Ticket-creating handoff path exists). The one WhatsApp-labeled surface, src/routes/_authenticated/admin.integrations.tsx:82-93 (the "WhatsApp Business" provider card) plus its handleAction at line 182-206, is a fully client-side fabricated mock: hardcoded queue depth/dead-letter/conflict counts and canned "4 hrs ago" timestamps in a useState array, pause/resume/disconnect buttons that only `setTimeout(800ms)` then mutate local state and show a toast — zero server calls, zero real inbound/outbound message flow. | S2 |
| 12b | §12.2/§12.4: The in-app Assistant must (among other things): explain the current screen/field/role/status/next step; answer product/pricing-policy/training/process/support questions; retrieve authorised Deals, Customers, Partners, Tasks, Tickets, courses, rewards, shipments, News, and Activity; compare metrics; convert requests into visible structured filters; summarise a record/timeline; draft a Deal, Lead, Customer, Contact, Task, Ticket, note, or News post; suggest missing data/next actions; navigate to a record/filtered view; and explain why access is unavailable. Conversation retention, deletion, and training-use policy must be visible and configurable under governance. | The shipped Assistant implements exactly two intents and nothing else. src/server/assistant.server.ts:27-39's SYSTEM_PROMPT instructs the model: "You ONLY help with two things: (1) drafting a brand-new sales Deal ... and (2) describing or listing the user's own existing Deals. Refuse anything else (other CRM objects, deleting anything, approvals, role changes, exports, unrelated chit-chat)." `parseIntent` (lines 75-101) only ever returns `list_deals`, `create_deal_draft`, or `none`, and `fetchScopedDeals` (lines 139-167) only ever queries `portal_deals`. There is no code path that retrieves Customers, Partners, Tasks, Tickets, courses, rewards, shipments, News, or Activity; no screen/field/role explanation; no record/timeline summarisation; no drafting of Lead/Customer/Contact/Task/Ticket/note/News; no navigation-to-record; and no cross-context metric comparison. The one filtering capability that does exist (stage/status extracted from free text) is not surfaced as a visible/persisted structured filter per §12.2 — it only produces an ephemeral list embedded inside a chat bubble in assistant-panel.tsx:143-162, never applied to the actual Pipeline view's filter controls. Separately, no governance UI anywhere exposes conversation retention/deletion/training-use policy for the Assistant (`grep -rln -i "assistant" src/routes/ src/components/` returns only assistant-panel.tsx and app-shell.tsx, no governance/settings route references it at all), so that §12.4 requirement has no surface at all. | S2 |

---


## 12. Chapter 13 — Support and ticketing

Beyond the S1 items in §2.13, §2.14:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 13c | §13.1/§13.2/§13.3: Every Ticket must carry category, Customer, active context, severity (distinct from priority), attachments, one-or-more product rows each with serial(s)/quantity/warranty/symptom/site-info/shipment-link/evidence, related Deal/Task/Shipment/certificate links, and a real assigned Support user/team plus watcher/escalation-owner participants. | support_tickets (db/schema.sql:1135-1151, plus ALTERs at 1580-1586/1642) only has partner_id, created_by, subject, description, status, priority, assignee_name (free TEXT, not a profiles/team reference), response_due_at, resolve_due_at, and the non-functional product_sku/serial_number pair (see 13a — and even if fixed, that's a single SKU+serial per ticket, not "one or more product rows" with "zero or more serials per product"). There is no category column, no severity column, no customer_id/contact, no active_context_id, no attachment table/column, no related-Deal/Task/Shipment/certificate linkage, and no participants table anywhere (no watchers, no escalation-owner). ticket-commands.server.ts and support.tsx confirm none of this exists in code either — SupportTicketRecord in src/lib/portal-records.ts:212-230 has the identical, matching, minimal field set. | S2 |
| 13d | §13.7: SLA policy may vary by severity, Partner tier, product, country/timezone, support entitlement, and business hours; the system tracks first-response, waiting start/end, reopen-request, and reopen-decision timestamps, and shows breach risk/paused conditions with effective-dated SLA snapshots. | ticket-commands.server.ts:253-260 computes SLA with one hardcoded branch (`priority === "high" ? 4h response / 1d resolve : 24h / 3d`) baked directly into createTicket() — no policy table, no variance by tier/product/country/business-hours/entitlement. Only response_due_at and resolve_due_at are ever stored; there is no first_response_at, waiting_started_at/ended_at, or reopen_requested_at/decided_at column in support_tickets, so most of §13.7's required tracked timestamps don't exist. The only breach indicator is a client-side `new Date(resolve_due_at) < new Date()` compare in support.tsx (lines 432-437, 486-502) — no pause logic, no business-hours awareness, no escalation-owner display. | S2 |
| 13e | §13.9: Notify the requester plus assigned/tagged Support participants, watchers, and escalation participants on creation, assignment, public reply, Waiting-on-Partner, SLA risk/breach, closure, reopen request, and reopen decision. | recordTicketEvent() (ticket-commands.server.ts:161-205) only writes to domain_activity_events and appends an envelope to command_outbox; nothing in the codebase ever consumes that outbox to populate the `notifications` table — a repo-wide grep for `INSERT INTO notifications` returns zero matches anywhere in src/. The only feedback on any ticket event is a toast shown in the acting user's own browser (support.tsx's toast.success/toast.error calls); no requester, assignee, watcher, or escalation owner is ever notified of any ticket lifecycle event by any other means. | S2 |
| 13f | §13.1/§13.8: Human-readable ticket numbers use format `LIV-SUP-YYYY-######`; the workspace offers clickable statistics, structured filters (status/severity/priority/SLA/Partner/Customer/country/product/assignee/date), queue/saved views, and an Activity/SLA timeline — and explicitly "There is no free-text search bar." | db/schema.sql:1137/1586 generates human_id as `'TICK-' \|\| LPAD(nextval(...), 4, '0')` (e.g. "TICK-0001"), not the canonical LIV-SUP-YYYY-###### format. support.tsx:394-399 ships a free-text "Search tickets" Input filtering client-side over subject/description/status/priority — the exact kind of control the blueprint says must not exist — while none of the mandated structured filters, clickable stats, saved views, or an Activity/SLA timeline component exist anywhere in the route; the workspace is just a reverse-chronological ticket list plus this search box. | S3 |

---


## 13. Chapter 14 — Insight Hub

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 14a | §14.5/§14.6/§14.8: Progress must flow Not Started → In Progress → Content Complete → Assessment Required → Passed → Certified, driven by a learner actually consuming a Lesson's video/text/resources and then submitting a final assessment; §14.8 requires the learner home's 'Continue Learning' to be a live action into that content. | There is no lesson-viewing or assessment-taking UI anywhere in the app — only two routes exist for the whole feature (src/routes/_authenticated/insight-hub.tsx and admin.learning.tsx), and neither renders a Lesson's video/text/resources or an assessment form; no third route (lesson detail, assessment, certificate) exists in src/routes. Once enrolled, insight-hub.tsx's 'Continue learning' button is hardcoded disabled (insight-hub.tsx:352-357: `<Button className="w-full" disabled><Play .../>Continue learning</Button>`), so a learner can never progress past the 'in_progress' status set at enrollment. This isn't just a missing button: `submitAssessmentAttempt` (src/server/learning-commands.server.ts:33-99) has zero call sites in any route/component in the repo — only its own definition and the createServerFn passthrough in src/integrations/local/learning-commands.ts reference it. Content Complete, Assessment Required, Passed, and Certified are unreachable through the product for any real learner. | S2 |
| 14b | §14.5/§14.6/§14.7: Progress needs the full 10-state machine (including Failed—Retry Available/Attempts Exhausted, Expired, Superseded) with guarded transitions; certification requires all required Lessons complete + prerequisites satisfied + a submitted assessment scoring ≥80% under a snapshotted attempt/cooldown policy (default 3 attempts, 24h cooldown before attempt 3, every attempt storing content version + selected questions); a Certificate must be a real entity (number, learner/affiliation snapshot, version, issue/expiry date, score, issuer, QR/verification URL, hash, PDF) with its own Active/Expired/Revoked/Superseded lifecycle and a public verification surface that hides the learner's profile. | learning_enrollments (db/schema.sql:1306-1318) only ever holds status 'in_progress'/'completed' (set at learning-commands.server.ts:79 and :159) plus a bare progress_percent, certificate_token and is_certified boolean — none of the 10 canonical progress states or 4 certificate statuses exist, and there is no status-transition guard anywhere. learning_assessment_attempts (db/schema.sql:1545-1552) has no content_version or selected_questions columns, and no learning_assessments question-bank table exists at all (confirmed by grep — learning_assessments has zero INSERT/UPDATE call sites anywhere in src/, only a single SELECT in learning-commands.server.ts:42). submitAssessmentAttempt (learning-commands.server.ts:33-99) certifies solely on `input.data.score >= assessment.passing_score`, where score is a raw number taken directly from the caller with no computation from real question responses, no check of prior attempt count, no cooldown, and no check that any required Lesson or prerequisite was completed (there is no lesson-completion tracking table at all — progress_percent is never incrementally updated, it is only ever hardcoded to 100 on pass). There is no Certificate record anywhere beyond the certificate_token UUID string, no certificate number/QR/hash/PDF, and no verification route exists in src/routes. handleViewCertificate (insight-hub.tsx:128-136) only pops a toast containing the raw token. | S2 |
| 14c | §14.4/§14.9: Administration must support full content authoring (create/preview/version/publish/retire Lessons, question-bank/assessment authoring, resource upload, transcript/caption validation), assignment/bulk-assignment with audience targeting by Global/Sales Region/Country/Partner/role/product authorisation/cohort/named user plus due date and reminder policy, certificate template/verification management, and completion/score/attempt/certification analytics with Partner/Region/Role/Product drill-down. | admin.learning.tsx only implements Track CRUD + publish toggle and Subject CRUD (saveTrack/toggleTrackPublish/saveSubject, lines 151-257); its 'Add lesson' button is hardcoded disabled (admin.learning.tsx:477-480: `<Button size="sm" variant="outline" disabled><Plus .../>Add lesson</Button>`), so lessons can be listed but never created or edited despite the learning_lessons table existing and being populated only by seed data. There is no question-bank/assessment authoring UI, no resource-upload UI, no assignment or bulk-assignment UI, and no analytics/drill-down UI anywhere in the codebase. The only 'audience targeting' mechanism that exists is a single tier_requirement string per Track (TIER_OPTIONS at admin.learning.tsx:65) — none of Global/Sales Region/Country/Partner/role/product-authorisation/cohort/named-user targeting, due dates, required/optional assignment, or reminder policy from §14.4 exist; enrollment is purely self-service opt-in (enrollInTrack, learning-commands.server.ts:106-173) with no Partner Admin assignment path despite §14.4 calling for one. Separately, admin.learning.tsx:95 hardcodes `if (!hasRole("super_admin"))` to gate the whole page, while the role_permissions seed data (db/schema.sql:1429: `('rm', 'learning', true, true, true, false)`) declares 'rm' has create/update rights on the 'learning' feature — that declared capability is dead code, never consulted by this page. | S2 |

---


## 14. Chapter 15 — Rewards

Beyond the S1 items in §2.15, §2.16:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 15c | §15.2/§15.4: Reward basis must be sum(qty×DTP) for reward-eligible lines only; reward USD value (basis×5%) must then be converted to points via an effective-dated, non-overlapping global points-per-reward-dollar policy (with an exception queue when no rate resolves); each award must snapshot eligible lines, DTP totals, the 5% rate, the points rate/policy version, and rounding result. | freezePricingRevision's total_dtp_usd (src/server/pricing-commands.server.ts:253-258) is `SUM(dtp_usd * quantity)` over ALL deal_line_items with no `WHERE reward_eligible = TRUE` filter, even though deal_line_items.reward_eligible exists (db/schema.sql:1224). awardApprovedWinRewards then uses that whole-deal total directly as `dealAmountBasis` (outcome-review-commands.server.ts:57-62) and calculateDealRewardAllocations (src/lib/deal-collaboration.ts:128-166) computes points as a single `round(dealAmount * reward_rate_percent / 100)` — collapsing the required two-stage (USD-value, then points-per-dollar-rate) conversion into one field, with no effective-dated points-policy table anywhere in the schema (grep for points-rate/points-policy tables returns nothing) and no reward exception queue. reward_point_events (db/schema.sql:684-696) stores only points_delta/reason — none of the required snapshot fields (eligible lines, DTP totals, USD value, rate/policy version, rounding result, idempotency key, reversal relationship) exist. | S2 |
| 15d | §15.9: Super Admin gadget catalogue must manage product/brand, image, description, points price, inventory, country eligibility, shipping requirement, active dates, and a fulfillment Task assignee/accountable LIVEY role; manual fulfillment must capture status, evidence, actor, and tracking where shipped. | reward_catalog_items (db/schema.sql:670-682) only has title, description, image_path, category, points_cost, stock, availability — no brand, country eligibility, shipping-requirement flag, active-date window, or fulfillment Task/accountable-role fields exist anywhere in the schema or in admin.rewards.tsx's create/edit form (lines 714-856, 959-1093). approveRedemption (admin.rewards.tsx:357-412) only flips status to 'approved'; there is no evidence/tracking/carrier capture for manually-fulfilled physical rewards anywhere. | S2 |
| 15e | §15.5/§15.6: Partner tier (Registered/Silver/Gold/Platinum, driven by governed commercial-performance qualification measures) must remain separate from reward points, and the Reward Store should surface tier/benefits where appropriate. | The Reward Store's 'Current tier'/'Your standing' display (src/routes/_authenticated/rewards.tsx:475-489) is computed entirely from cumulative reward-points thresholds via rewardTierForPoints (src/lib/rewards.ts:5-10, 84-116 — Bronze/Silver/Gold/Platinum at 0/500/1500/3000 points), which is a different concept from the real governed `partners.tier` enum (db/schema.sql:56,139) that admin.partners.tsx sets from turnover band (tierForTurnover, admin.partners.tsx:132) and that price_books actually uses for PTP adjustment (src/lib/catalog.ts partner_tier). The Reward Store never shows the real governed tier at all, instead presenting a same-named but unrelated points-gamification tier that directly derives from reward points — the opposite of 'remains separate from reward points'. | S3 |
| 15f | §15.10/§15.11: Super Admin statistics cards must open their underlying filtered data, and catalogue CSV/XLSX import must report row-level results. | The `Metric` component in admin.rewards.tsx (lines 1251-1261, used at 572-592) renders a static, non-interactive Card with no onClick/navigation — clicking a stat does nothing. Separately, validateRewardCatalogImportRows (src/lib/reward-admin.ts:114-161) is all-or-nothing: if any row fails validation it returns `{ ok: false, rows: [] }` and rejects the entire file, so valid rows in a batch are never imported alongside per-row error messages for the invalid ones. | S3 |

---


## 15. Chapter 16 — Analytics, imports, exports, settings

Beyond the S1 items in §2.17, §2.18:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 16b | §16.2: Exports must include generated timestamp/actor/scope/filters/freshness, be audited, expire when delivered by link, and provide progress/durable results for large jobs; operational lists must support both CSV and XLSX. | No export path calls recordAuditEvent: CsvExportButton (src/components/csv-export-button.tsx:28-40) just builds a CSV in-browser and triggers a blob download via downloadCsv (src/lib/csv-export.ts:40-48), with no audit call anywhere in it, settings-export-card.tsx, or export-registry.ts. buildCsv (csv-export.ts:33-38) writes only header+rows — no generated-timestamp/actor/scope/filter/freshness metadata is embedded anywhere in the file (only the filename gets a date stamp via buildExportFilename). The Analytics XLSX export (src/lib/analytics-export.ts:27-41) does embed `generated_at` in a Summary sheet but still no actor/scope/filters and is likewise never audited. All exports are synchronous client-side blob generation — there is no async job, progress indicator, durable result, or link-delivery/expiry mechanism for any export, so the "Large/bulk data: Asynchronous CSV/XLSX archive" row of §16.2's format table has no implementation. Operational-list datasets in export-registry.ts (deals, customers, notifications, rewards, etc.) are CSV-only — no XLSX option exists for any of them, contrary to the format table's "Operational lists: CSV, XLSX" row. | S2 |
| 16c | §16.3: Every import must provide a dry-run preview, a separate confirm step, row-level results after the run, and an audit/rollback-compensation strategy. | Every import surface (deals, customers, team, users, catalog) validates a file and commits it in the same handler invocation with no preview/confirm gate: e.g. deals.tsx's handleImportFile (src/routes/_authenticated/deals.tsx:961-1271) parses, validates, resolves FX, inserts customers, inserts deals, and records one audit event all in one pass triggered purely by selecting the file — there is no intermediate "review N rows, click Confirm" screen. The same immediate-commit pattern repeats in customers.tsx:626-643 (validateCustomerImportRows → insert), partner.team.tsx:279-286 (validateTeamImportRows → createPartnerTeamMembersBulk), admin.users.tsx:407-443 (importUsers: validate → createWorkspaceUsersBulk), and admin.catalog.tsx:244+ (handleImportFile). Post-commit feedback is only an aggregate count (`buildImportSummaryMessage`/toast), never a per-row created/skipped/error report. If a later step fails after an earlier one succeeded there is no compensation: deals.tsx inserts new `portal_customers` rows (lines 1201-1206) before inserting the deal rows (line 1244) with no rollback if the deal insert subsequently throws, leaving orphan customer records. Audit logging is also inconsistent: only deals.tsx's import calls recordAuditEvent (line 1251); customers.tsx, partner.team.tsx, admin.users.tsx, and admin.catalog.tsx's import handlers never call recordAuditEvent at all. | S2 |
| 16d | §16.1/§16.6: Analytics must cover 15 named report groups with broad governed filters (date basis, Region, Country, Province/State, Partner, Customer, user, role, product, tier, stage, status), and all statistics cards/chart segments that represent records must be clickable into the underlying scoped view. | src/routes/_authenticated/analytics.tsx implements only a small subset of the 15 required report groups — pipeline value/won/lost/open/win-rate (lines 133-148), deal stage mix (150-158), win/loss funnel (160-164), customer health bands (168-183), and catalog mix by tier (185-192). There is no partner growth/approval funnel, weighted pipeline, stage-conversion/time-in-stage, loss-reason breakdown, product/series mix, pricing waterfall/discount, regional/country performance breakdown, partner/user contribution, task productivity/overdue, news reach/acknowledgement, support SLA/serial trends, learning completion/certification, points liability/awards/reversals/redemptions, Auto CRM funnel, or integration/shipment performance anywhere on this page (or elsewhere, per grep for equivalent chart/report logic). Filtering is limited to a single region selector (`useRegionFilter`, line 23/40); there is no date-range, country/province, partner, customer, user, role, product, tier, stage, or status filter control on the page. None of the `Metric` cards (lines 260-268, component at 445-455) or chart segments (stageData/winLossFunnel/healthBands/catalogByTier renders, lines 288-406) have an onClick handler, so none of them open the underlying scoped view, directly contradicting the acceptance criterion "Statistics cards are clickable." | S2 |
| 16e | §16.4: Settings IA must give all users Personal Profile, Security+active sessions, accessibility, locale/timezone/reference currency, in-app notification preferences, email/WhatsApp digest preferences+consent, and a read-only authorised-contexts view; Partner Admin needs Company/team-defaults/context-prefs/comm-settings; Super Admin needs a consolidated hierarchy/roles/policy/thresholds/stage-templates/price-books/reward-policy/support-SLA/news-governance/integrations/security-retention/export-governance surface. | src/routes/_authenticated/settings.tsx (H1 literally "Export hub", line 206; body copy "Every dataset downloads as its own CSV", line 207-210) still consists of nothing but the CSV/XLSX export catalog (SectionBlock groups, lines 320-356), a password-change card (250-316), and — super_admin only — a single Zoho Sign integration card (358-386). Confirmed absent anywhere in the app (grep across src for accessibility/timezone/active-session/notification-preference/digest/WhatsApp-consent all returned no hits outside an admin-only WhatsApp Business integration toggle in admin.integrations.tsx:82-83): Personal Profile editing (no full_name/phone/avatar edit fields exist; the header's "Profile & settings" menu item in app-shell.tsx:191-195 routes here and finds none), active-session management, accessibility settings, locale/timezone/reference-currency preferences, in-app notification preferences (notifications.tsx is only a read feed, not a preferences page), and email/WhatsApp digest preferences+consent. Partner Admin's required Company/team-defaults/context-preference/comm-settings items and Super Admin's required approval-thresholds/stage-and-task-template/support-SLA/security-retention/export-governance items are likewise not present in Settings — related capabilities that do exist (roles, catalog, rewards, news, integrations) live only on separate /admin/* routes outside the Settings IA the blueprint specifies. | S2 |
| 16g | §16.5: High-impact policy changes must be effective-dated and versioned, with an impact preview identifying affected users/workflows, and dual approval for security/points/price-policy changes. | No such capability exists anywhere: repo-wide case-insensitive search for effective_date/effective-dated/versioned/policy_version/dual-approval/second-approver/impact-preview/"affected users" returns zero matches in any .ts/.tsx/.sql file. Role/permission changes (identity.change_user_role), reward point-rate/policy edits (admin.rewards.tsx), and price-book edits (admin.catalog.tsx) all take effect immediately on save with a single approver and no preview of who/what is affected. | S2 |

---


## 16. Chapter 17 — External integrations

Beyond the S1 items in §2.19, §2.20, §2.21:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 17d | §17.5 (entire Zoho Books section, source-of-truth contract 17.5.1): Zoho Books must be the accounting system of record with controlled two-way sync of Contacts, Sales Orders, Invoices, Payments, Credit Notes, plus a read-only Financial panel on the deal and reconciliation. | There is no Zoho Books integration anywhere in the codebase. grep across src for zoho.?books/sales.?order/invoice-related accounting API code returns nothing; there is no Financial panel component, no external_link/contact-sync table usage, and no outbound sales-order/invoice creation path. §17.5 is entirely unbuilt — deals have no accounting linkage of any kind beyond the free-text deal amount fields already covered by pricing/deal chapters. | S2 |
| 17e | §17.6 (WhatsApp), §17.7 (Email delivery), §17.8 (GyFTR voucher order), §17.9 (DHL), §17.1 rule 4 (transactional outbox): WhatsApp inbound/outbound workflows, digest email delivery, GyFTR's reserve-then-settle voucher order lifecycle, and DHL shipment creation/tracking must each exist as durable, idempotent provider adapters, with outbound work starting from the transactional outbox. | No server-side adapter exists for WhatsApp, email delivery, or DHL anywhere in the repo (grep for whatsapp/postmark/sendgrid/nodemailer/smtp/dhl across src returns no real implementation — only the fabricated card labels in admin.integrations.tsx). GyFTR has one client, src/integrations/gyftr/gyftr-client.ts (issueGyFTRVoucher, lines 32-90), but grep confirms it has zero callers anywhere else in src — it is dead code. The actual reward-redemption path is: rewards.tsx submitRequest (lines 226-272) does a raw `supabase.from("reward_redemptions").insert(...)` with status 'requested', and admin.rewards.tsx approveRedemption (lines 357-412) does two independent client-issued writes — update redemption status to 'approved', then insert a negative reward_point_events row — with no provider order, no idempotency key, no points reservation, and no voucher ever issued or displayed. Separately, command_outbox (db/schema.sql:766-783) and command_inbox (785-799) exist and are populated only by appendOutboxEnvelope (src/server/command-runtime.server.ts:23-67, INSERT ... ON CONFLICT DO NOTHING) from several domain command files — but nothing anywhere in the repo ever SELECTs pending rows from command_outbox to dispatch them (confirmed by repo-wide grep; the only other reference is inventory-report.ts's schema-consistency listing, not a worker). The transactional-outbox mechanism this chapter requires has no drain/worker at all, for any provider. | S2 |
| 17f | §17.4.1 point 6, §17.4.2 (stored fields, one-active-request rule, resend/replace semantics): The portal must retrieve and store the signed artifact and completion evidence on Signed, keep source/signed document checksums, enforce one active signing request per partner-application revision, and treat resend/replace as creating a new linked Agreement revision rather than silently overwriting the prior request. | There is no distinct 'Agreement' entity at all — state lives as a handful of columns directly on `partners` (agreement_envelope_id, agreement_sent_at, agreement_signed_at, agreement_provider, status), with no revision history and no stored checksums anywhere. storeSourceAgreementUpload (src/server/zoho-api.server.ts:195-248) stores only the pre-signature source PDF; on the webhook's 'completed' status, markPartnerSignedPendingReview (lines 175-193) only flips status fields — no code anywhere fetches or stores the signed document/completion certificate from Zoho Sign. handleZohoSendAgreement (lines 250-351) also never checks for an existing active agreement_envelope_id before calling sendAgreement — every send unconditionally creates a brand-new Zoho Sign request and overwrites the prior envelope ID (line 314-323) without cancelling or expiring the old one at the provider. | S2 |

---


## 17. Chapter 18 — Canonical data model

Beyond the S1 items in §2.22:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 18c — **bounded step taken 2026-08-06; still fundamentally duplicated** | §18.1/§18.2/§18.10 (invariant #9): One canonical data model must serve the whole product. The glossary defines 'Product / Product Variant' (governed catalogue identity, SKU, brand/series) and 'Price Book / Price Row' (effective-dated MSRP and partner-tier transfer prices) as the authoritative Catalogue/Pricing domain (§18.10), and invariant #9 requires money to be fixed-point decimal with an explicit ISO currency code. | db/schema.sql defines products, product_variants, product_skus, combos, combo_components, price_books, price_rows, fx_snapshots (lines 485-624) with proper NUMERIC(18,4) money columns and effective-dating — but a repo-wide grep shows zero application code (routes, server functions, components) ever queries or writes any of these tables; schema.sql's own comment at lines 1650-1661 states 'Every one of these tables has zero rows on this database.' src/lib/pricing-domain.ts defines the matching ProductRecord/PriceBookRecord/etc. types and a validatePricingImportRows function, but that function is imported by src/lib/catalog.ts:3 and never called anywhere in that file (dead import). The actual live catalog feature (admin.catalog.tsx, dropdown-sources.server.ts, inventory-report.ts, analytics.tsx, export-registry.ts, table-policy.server.ts) exclusively reads/writes portal_catalog_items, whose list_price and margin columns are TEXT (db/schema.sql lines 468-469), not fixed-point decimal with an ISO currency code. **Fix (bounded):** the new "Add line item" UI (9f/18d) reads its product/price options through a new `listLineItemCatalogOptions` (`dropdown-sources.server.ts`), which queries `product_skus`/`product_variants`/`products` first and only falls back to `portal_catalog_items` when the governed tables have zero active rows — so this is now real, tested application code that reads the governed tables, not zero. **Still open — the vast majority of this finding:** nothing writes to or backfills the governed tables (they remain literally empty on every environment, so this fallback is what actually runs today); `admin.catalog.tsx`, `inventory-report.ts`, `analytics.tsx`, `export-registry.ts`, and the generic `source="catalog"` dropdown used everywhere else in the app are all completely untouched and still exclusively read/write `portal_catalog_items`'s TEXT `list_price`/`margin` columns; no migration, dual-write, or reconciliation between the two catalogues exists. This is one new bounded reader, not a migration — the duplicate live catalog is still the system of record for everything except this one new form. | S2 |
| 18d — **fixed 2026-08-06** | §18.2/§18.5 (Deal Line Item entity; command contract): The 'Deal Line Item' is a canonical entity (product, quantity, price snapshots, discount, proposed price, margin) that domain commands must populate through named commands, with a Pricing Revision created from it. | src/server/pricing-commands.server.ts:24-83 defines addDealLineItem and it is wired end-to-end (src/integrations/local/pricing-commands.ts:23-29), but no UI component anywhere calls it — src/components/deal-line-items.tsx only lists (line 33-40), removes (line 50-60), and freezes (line 62-71) line items; there is no add/create form. src/server/deal-commands.server.ts (deal creation) never inserts into deal_line_items either. Because the Freeze button is gated on `items.length > 0` (deal-line-items.tsx line 84) and items can never be added, the button never renders, so pricing_revisions can never actually be created through the shipped product — the entire per-item pricing / frozen-DTP-total mechanism that outcome-review-commands.server.ts's awardApprovedWinRewards prefers (lines 48-62) is unreachable dead code in practice. **Fix:** same "Add line item" dialog as 9f — `deal-line-items.tsx` now calls `addDealLineItem` with the exact input shape the command expects (productId, quantity, msrpUsd, ptpUsd, discountPct, dtpUsd, proposedSellingPriceUsd, rewardEligible). Items can now be added, so the Freeze Pricing button's `items.length > 0` gate can actually pass, and `pricing_revisions` can now be created through the shipped product — closing the "unreachable dead code" consequence this finding described. `createDeal` still never inserts line items directly (matches 9f's still-open scope: it's an optional post-creation step, not part of deal creation itself). | S2 |
| 18f | §18.4 invariant #18; §18.5 command contract; §18.11: 'Every retryable command has an idempotency key and explicit terminal/unknown status handling' (invariant #18); the domain command contract (§18.5) requires an idempotency key for externally replayable entry points; §18.11 requires 'a duplicate command or webhook produces one logical result.' | src/domain/contracts/commands.ts defines a full CommandEnvelope/OutboxEnvelope contract with an idempotencyKey field and createCommandEnvelope/createOutboxEnvelope constructors, and src/server/command-runtime.server.ts defines persistCommandMutationAndOutbox that accepts a full CommandEnvelope — but persistCommandMutationAndOutbox and createCommandEnvelope are never called by any real command handler (only referenced in their own test file). Every actual command module hardcodes idempotencyKey: null when building its OutboxEnvelope: src/server/deal-commands.server.ts:236,662; src/server/task-commands.server.ts:204; src/server/ticket-commands.server.ts:200; src/server/user-role-commands.server.ts:169; src/server/role-assignment-commands.server.ts:295,379. command_outbox rows are deduplicated only on a freshly-generated outboxId (ON CONFLICT DO NOTHING in command-runtime.server.ts:47, always a new UUID per call), which provides no actual replay protection. Impact is partially mitigated where the underlying record carries an optimistic-concurrency version check (e.g. deal-commands.server.ts:285 checks deal.version against expectedVersion), but any version-less write (e.g. the addDealLineItem path in finding 18d, or any insert-only command) has no duplicate-submission protection at all. | S3 |

---


## 18. Chapter 19 — Security, privacy, reliability, performance

Beyond the S1 items in §2.23, §2.24:

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 19c | §19.2: MFA is mandatory for Super Admin and all LIVEY internal roles, mandatory for Partner Admin at full activation, and always available for Partner User; email verification is required before an onboarding application can be submitted. | No MFA implementation exists anywhere: repo-wide search for mfa/totp/2fa across src/ and db/schema.sql finds nothing but an unrelated OTP UI input component (src/components/ui/input-otp.tsx) and no mfa-related columns on profiles/sessions. signUpLocal (src/server/livey-service.server.ts:1504-1556) creates the profile, partner_admin role, and an authenticated session immediately with no email-verification step; no verification_token/email_verified column or flow exists anywhere in the schema or codebase. | S2 |
| 19d | §19.2 / §19.9: Credential stuffing, enumeration, automated signup, and token abuse must be rate-limited using account, IP, device, and behaviour signals; adaptive limits must protect auth, reset, verification, invitations, MFA, uploads/downloads, assistant calls, bulk commands, exports, and more. | No rate-limiting library, middleware, or in-house throttle exists anywhere in the repo (repo-wide search for express-rate-limit/rate-limiter/upstash/throttle/lockout/failed_login finds nothing). signInWithPassword (src/server/livey-service.server.ts:1449-1502) has no failed-attempt counter, account lockout, or per-IP/per-account throttle — password guessing, signup automation, and password-reset-request spam are all unbounded. | S2 |
| 19e | §19.8: Mandatory audit coverage includes sign-in, MFA, session, recovery, and context events. | portal_audit_events is only ever written through the client-triggered recordAuditEvent path (src/lib/workflow-events.ts:36, actor-stamped server-side in src/server/table-policy.server.ts:861-874 per this session's fix), called only from a handful of domain routes (rewards.tsx:249, admin.deals.tsx:329, admin.partners.tsx:341/504, admin.rewards.tsx:248/288/316/394/435, deals.tsx:1251/1509/1564, pipeline.tsx:248/296). signInWithPassword, signUpLocal, requestPasswordReset, completePasswordReset, signOutLocal, and revokeUserSessionsAndContexts in src/server/livey-service.server.ts never write an audit row — no sign-in, sign-in failure, password-reset request/completion, sign-out, MFA, or session-revocation event is ever recorded anywhere. | S2 |
| 19f | §19.5: Voucher codes, high-risk provider secrets, and equivalent sensitive values use separate field/envelope encryption; the browser policy enforces a restrictive CSP, an explicit CORS origin allowlist, and frame-ancestors clickjacking protection. | zoho_sign_tokens (db/schema.sql:266-274) stores access_token and refresh_token as plain TEXT with no encryption; src/lib/zoho-sign.ts reads/writes them in cleartext (lines 60-93, 125-171) — a long-lived Zoho Sign OAuth refresh token sits unencrypted in the database. Separately, src/server.ts — the single fetch entrypoint for every response — only ever sets a correlation-ID header (lines 60, 113-121) and never sets Content-Security-Policy, X-Frame-Options/frame-ancestors, or any CORS allowlist header anywhere in the codebase (confirmed by repo-wide search); the session cookie itself is Secure/HttpOnly/SameSite=Lax (signInWithPassword:1486-1492) but the surrounding browser-hardening headers this bullet requires are entirely absent. | S2 |
| 19g | §19.6 / §19.7: A policy-driven, auditable, resumable expiry job per the §19.7 retention table, plus a data-subject correction/access/portability/erasure workflow that locates the person, classifies fields, previews effect, and executes idempotently. | No expiry/purge job, no anonymization routine, and no data-subject request workflow exist anywhere in src/ — a repo-wide case-insensitive search for retention/anonymi.../data-subject/erasure/gdpr returns nothing but one unrelated false-positive match (a `data.subject` field-name collision in src/server/ticket-commands.server.ts:233). None of the §19.7 retention periods are implemented as scheduled or enforced logic anywhere. | S2 |
| 19h | §19.11 / §19.12: PITR-backed backups with quarterly isolated restore drills against the 4-hour RTO; required telemetry across requests/auth/queues/providers/webhooks/assistant/SLA/exports; alerts with severity, accountable role, runbook, and escalation path. | No backup automation, restore scripts, CI/CD, or infrastructure-as-code exist in the repo (no *.yml/*.yaml files, no backup-related scripts found repo-wide). No observability/telemetry library is wired in — a repo-wide search for sentry/datadog/prometheus/opentelemetry/otel finds zero matches; only a correlation-ID header pass-through exists (src/server.ts:60, 113-121). No alerting configuration of any kind is present anywhere. | S2 |
| 19i | §19.14: Release evidence requires automated scans on every priority route, manual keyboard-only and screen-reader testing, 200%/400% zoom-reflow, contrast verification, reduced-motion testing, and regression tests for shared components; "an automated scan alone is not sufficient evidence of WCAG 2.2 AA." | No axe-core/jest-axe or any automated accessibility-scanning tool is installed — package.json only lists eslint-plugin-jsx-a11y, a static lint rule set, not a runtime/e2e scanner. The only e2e tests labeled "accessible" (e2e/deal-pipeline.spec.ts:39-45 "deal registration form is accessible", lines 61-64 "pipeline view is accessible") merely assert that a dialog/heading becomes visible after a click — they perform no keyboard-only traversal, no screen-reader check, no contrast/zoom/reduced-motion validation, and no axe scan. None of the required §19.14 release evidence exists, and the test names materially overstate what is actually verified. | S2 |
| 19j | §19.13.2: "All large lists paginate with stable cursor or deterministic keyset semantics... N+1 record policy checks are eliminated without weakening policy." | List routes fetch entire tables with no limit/cursor: deals.tsx's load() (src/routes/_authenticated/deals.tsx:339-357) runs an unbounded `portal_deals.select("*")` plus full unbounded `portal_deal_collaborators` and `portal_team_members` selects; customers.tsx's load() (src/routes/_authenticated/customers.tsx:156-167) does the same for portal_customers/portal_customer_activities/customer_participants. None use `.limit()`/`.range()` even though the local client shim supports `.limit()` (src/integrations/local/client.ts:341) — against the §19.13.3 baseline (1,000,000 deals, 500,000 customers) this loads the full table into the browser on every page view. Separately, table-policy.server.ts's linked-deal/linked-ticket scope checks run one sequential DB round-trip per row instead of batching: `for (const dealId of dealIds) { await assertLinkedDealAccess(dealId, auth); }` (lines 794-796, mirrored for tickets at 831-833), each invoking a fresh `SELECT ... FROM portal_deals WHERE id = $1` (lines 455-461) — a genuine N+1 pattern for any bulk insert of rows sharing one deal_id/ticket_id, such as the up-to-100 deal_line_items on a single deal contemplated by the §19.13.3 profile. | S2 |

---


## 19. Chapter 23 — Governed defaults and reference data

| # | Spec | Shipped | Sev |
| --- | --- | --- | --- |
| 23a | §23.1 rule 1 / §23.4 (all subsections) / §23.9 invariant 1: §23.1 declares this section 'the implementation dictionary for labels and stable keys used throughout the blueprint' and requires every governed value to have a stable machine key living in one explicitly named sole registry (§23.9: 'every canonical term has one definition or one explicitly named sole registry'). §23.4 lists 17 canonical dictionaries with exact machine keys: Assignment status, Distributor fulfillment, Partner lifecycle, Deal registration, Deal pipeline/probability, Deal outcome review, Task, Coverage Exception, Ticket, Lead/Auto CRM, Learning progress, Certificate status, News, External delivery, Agreement status, Redemption, Shipment. | Re-verified key-by-key against src/domain/contracts/taxonomy.ts (the file this repo treats as the canonical contract, feeding src/domain/contracts/reference-data.ts's GOVERNED_REFERENCE_BUCKETS which scripts/bootstrap-db.ts's seedGovernedReferenceData() persists into the live lookup_values table). Only 2 of 17 dictionaries match blueprint exactly: Assignment status (taxonomy.ts:39-45) and Deal pipeline (taxonomy.ts:63-72). Every other dictionary present is renamed/reduced and several have no constant at all. Concrete tallies: Partner lifecycle — blueprint 13 keys (email_unverified, profile_incomplete, draft, submitted, under_review, changes_requested, partial_approval, agreement_pending, signed_pending_review, approved, rejected, suspended, offboarded) vs taxonomy.ts:49-59 PARTNER_LIFECYCLE_STATUSES (9 keys); only 6 match, and suspended/offboarded/email_unverified/profile_incomplete/draft are missing outright (changes_requested→need_more_info, agreement_pending→pending_agreement). Deal registration — blueprint 7 keys (draft, submitted_for_review, auto_approved, approved, changes_requested, rejected, cancelled) vs taxonomy.ts:76-85 REGISTRATION_STATUSES (6 keys: draft, submitted, under_review, approved, rejected, need_more_info); only draft/approved/rejected match (3/7), auto_approved and cancelled are absent entirely, and 'under_review' is not even a blueprint key for this dictionary. Deal outcome review — blueprint 6 keys (not_applicable, po_pending, under_livey_review, changes_requested, approved_won, rejected_outcome) vs taxonomy.ts:97-106 PO_REVIEW_STATUSES (not_required, requested, received, verified, rejected, closed) — 0 of 6 keys match. Redemption — blueprint 8 keys (requested, points_reserved, pending_review, processing, fulfilled, failed, cancelled, refunded) vs taxonomy.ts:147-155 REWARD_STATUSES (requested, approved, fulfilled, rejected, canceled) — only 2/8 match (requested, fulfilled); points_reserved/pending_review/processing/failed/refunded are absent, and this is not just a paper mismatch: the live reward_redemptions.status column (db/schema.sql:704) is only ever set to 'requested' (default) / 'approved' / 'rejected' by src/routes/_authenticated/admin.rewards.tsx:365,421 — confirmed via a direct query against the live database, which returned only 'requested' as the distinct value present. Lead/Auto CRM — blueprint 10 keys vs taxonomy.ts:134-142 LEAD_STATUSES (5 keys: new, contacted, qualified, converted, disqualified) — only 4/10 match, and there is no 'leads' table anywhere in db/schema.sql, so the dictionary has no backing store at all. Learning progress — blueprint 10 keys describing per-learner progress vs taxonomy.ts:144-145 LEARNING_STATUSES = ['draft','published','archived'] which is actually a course-publishing lifecycle, not learner progress — 0/10 correspondence; the live learning_enrollments.status column (db/schema.sql:1310) only ever holds 'in_progress'/'completed' plus a separate is_certified boolean, so failed_retry_available/failed_attempts_exhausted/assessment_required/expired/superseded cannot be represented. Agreement status — blueprint 9 keys vs taxonomy.ts:169-177 AGREEMENT_STATUSES (7 keys, includes non-canonical 'pending_review'/'active') — only 4/9 match; queued/delivered/viewed/declined are absent, and the partners table (db/schema.sql:121-161) tracks agreement progress only via agreement_sent_at/agreement_signed_at timestamps, not a provider-evidence state machine. Ticket workflow — taxonomy.ts:121-130 TICKET_STATUSES (open, triaged, waiting_on_partner, waiting_on_livey, resolved, closed, reopened, canceled) matches only 3/5 blueprint keys and treats 'reopened' as a persistent status though the blueprint explicitly says reopened is an Activity event, not a state; separately, ticket priority is stored as 'critical' (src/server/inventory-report.ts:368) instead of blueprint's 'urgent', and no distinct severity field (s1_critical/s2_high/s3_medium/s4_low) exists anywhere in support_tickets (db/schema.sql:1135-1151) — that whole severity dimension is unbuilt. This is not an improvement over the prior audit's '10 of 16' finding; re-verification this session shows the true count is at least 15 of 17 dictionaries divergent or unrepresented, and nothing in this session's fix list touched taxonomy.ts. | S2 |
| 23b — **Coverage Exception's DB backing partially fixed 2026-08-06; the other 5 dictionaries untouched** | §23.4 (Coverage Exception workflow, Distributor fulfillment, Certificate status, News, External delivery, Shipment): §23.4 defines six additional canonical dictionaries as required capabilities: Coverage Exception workflow (open/in_remediation/resolved/cancelled), Distributor fulfillment (awaiting_stock/ready_to_dispatch/dispatched/delivered/exception), Certificate status (active/expired/revoked/superseded, authoritative over Learning Progress per §14.7), News (draft/scheduled/published/expired/archived), External delivery (9-state email/WhatsApp delivery-receipt tracking per §23.5's 'Routine external business notifications: Daily or weekly email/WhatsApp digest'), and Shipment (11-state carrier lifecycle). | None of these six have any implementation beyond, at most, an unused TypeScript constant. Coverage Exception and Distributor fulfillment have zero occurrences anywhere in src/ or db/ (grep for 'coverage_exception', 'in_remediation', 'awaiting_stock', 'ready_to_dispatch' returns nothing) — taxonomy.ts doesn't even declare a placeholder for either. Certificate status has no field of its own: learning_enrollments (db/schema.sql:1306-1318) has only certificate_token TEXT and is_certified BOOLEAN — a certificate can never be marked expired/revoked/superseded in this schema. News has no status column at all: portal_news_posts (db/schema.sql:657-669) was queried live and Postgres returned 'column "status" does not exist' — draft/scheduled/published/expired/archived cannot be represented. External delivery has no channel/delivery-state tracking: the notifications table (db/schema.sql:1122-1131) is in-app only (title/message/type/read boolean) with no email/WhatsApp send/delivered/read/failed/suppressed states, so the §23.5 'Routine external business notifications' capability this dictionary supports is absent. Shipment has no table at all in db/schema.sql (confirmed via full CREATE TABLE listing) despite taxonomy.ts:157-165 declaring a 7-key SHIPMENT_STATUSES that itself only overlaps 2 of the 11 canonical keys (delivered, returned) and invents non-canonical ones (pending, packed, shipped, lost). **Fix (Coverage Exception only, 2026-08-06, landed alongside 9g):** the 9g fix (see Chapter 9's table) added a real `coverage_exceptions` table (db/schema.sql) with a `status` column, and a matching `COVERAGE_EXCEPTION_STATUSES = ["open","in_remediation","resolved","cancelled"]` dictionary in taxonomy.ts — the exact 4 keys this finding names, closing the "taxonomy.ts doesn't even declare a placeholder" half of this specific dictionary's gap. **Still open, exactly as before:** only `"open"` is ever written (9g's block-on-missing-coverage path); no code transitions a Coverage Exception through `in_remediation`/`resolved`/`cancelled` — the remediation workflow this dictionary is *for* remains unbuilt. Distributor fulfillment, Certificate status, News, External delivery, and Shipment are completely untouched — still 5 of 6 dictionaries in this finding with zero backing. | S2 |
| 23c | §23.9 invariant 1 ('every canonical term has one definition or one explicitly named sole registry') / §23.4 Partner lifecycle: §23.9 requires exactly one authoritative definition per canonical term. §23.4's Partner lifecycle dictionary includes suspended and offboarded as post-approval administrative terminal states. | Two of the domains audited above (Task, Ticket) are actually implemented correctly against blueprint in their live command modules — but by design, not by fixing the registry: task-commands.server.ts:27-35 defines its own TASK_STATUSES = ['to_do','in_progress','blocked','completed','cancelled'] (matches blueprint's todo/in_progress/blocked/completed/cancelled almost exactly) and ticket-commands.server.ts:25-38 defines its own TICKET_STATUSES = ['open','in_progress','waiting_on_partner','closed','reopen_requested'] (exact match to blueprint §23.4 Ticket workflow). Both files contain explicit code comments stating they intentionally diverge from taxonomy.ts's 'generic placeholder' because it doesn't match the blueprint or the live DB values — i.e., the codebase itself documents that there is no single sole registry for these terms, contradicting §23.9's invariant, and creates a live risk that a future caller importing TASK_STATUSES/TICKET_STATUSES from domain/contracts/taxonomy.ts (as src/domain/contracts/state-machine.ts and reference-data.ts already do) gets the wrong values silently. Separately, Partner lifecycle's live backing store, the Postgres `partner_status` type (db/schema.sql:29-38, confirmed live via pg_enum query to currently hold pending_partner_registration/submitted/under_review/partial_approval/pending_agreement/signed_pending_review/need_more_info/approved/rejected), is a strict CREATE TYPE ... AS ENUM — it structurally cannot hold blueprint's `suspended` or `offboarded` values without an ALTER TYPE ADD VALUE migration; no such migration exists in db/schema.sql, so a partner can never be represented as administratively Suspended or Offboarded under the current schema, only Rejected/Approved. | S3 |

---
## 20. Phased remediation plan

This supersedes the old flat "suggested sequencing" list with named phases that cover the
**whole** backlog above (S1 through S3, all 17 substantive chapters), not just the 24 S1s. Each
phase has a scope (gap IDs from §2 and §3-§19 above, plus §21's new item), a rationale for why
it sits at that point in the sequence, and an exit criterion — a concrete, checkable statement
of "done," not "improved." Phases are ordered by dependency and blast radius: a later phase
routinely assumes an earlier phase's fix is already in place (e.g. Phase 4's reward fulfillment
work assumes Phase 2's scope-spec review already fixed who can act on a redemption). Do not
skip ahead on severity label alone — an S2 that blocks a later S1's fix belongs earlier.

Several items appear in more than one phase's scope on purpose: the underlying code is touched
incrementally (e.g. `table-policy.server.ts`'s scope-spec function is edited in Phase 2 for
tenant isolation and again in Phase 5 for notification recipients) rather than in one giant
patch, so each touch is reviewable and revertible on its own.

---

### Phase 0 — Close the exploitable holes (today, no architecture change needed)

**Status as of 2026-08-05: mostly done.** §2.3/§2.23 (password-reset token leak/enumeration),
§2.17 (password_hash leak), and §2.19 (Zoho Sign endpoint auth) are fixed and deployed. §2.20's
fail-open bug is fixed (webhook now rejects when unsigned/misconfigured, deployed with a real
secret in Railway — pending the one remaining manual step of configuring that same secret in
Zoho's own webhook dashboard, which no session has had credentials for).

**Scope still open:** the rest of §2.20 — timestamp/replay-window/provider-account checks, and
a durable `command_inbox` receipt persisted *before* any domain mutation runs (right now the
webhook goes straight from signature check to mutation with no receipt row at all). §2.3's
other half — an email-verification gate on registration — is also still open, and real email
delivery to replace the current log-relay stopgap is deferred to Phase 5 (it needs the same
transport digests need, no sense building it twice).

**Why first:** these are live, unauthenticated exposures with no dependency on anything else
in this plan — closing them is a config/check fix, not a redesign.

**Exit criterion:** no unauthenticated endpoint returns a credential, token, or account-existence
signal (met); every inbound webhook is signature *and* replay-window verified and leaves a
durable receipt in `command_inbox` before any domain mutation runs (signature check met,
replay-window and receipt still open).

---

### Phase 1 — Session and credential hardening

**Status as of 2026-08-05: partly done.** §2.24's core is fixed — session revocation is wired
to password reset, admin-forced reset, and role/assignment change, and absolute session
lifetime is now role-tiered (12h internal/Super/Partner Admin, 24h Partner User) instead of a
flat 14 days.

**Scope still open:** idle-timeout enforcement (30/60 min) — `last_seen_at` is tracked but
nothing compares it to a threshold yet; this needs the per-request session-validation path
(`getSessionFromToken`), deliberately deferred when the rest of §2.24 landed to keep that
batch's blast radius small. A user-facing "active sessions" list/revoke UI also doesn't exist
yet. Beyond that: §6c (inviter sets a new user's plaintext password directly, no forced reset,
no credential-status lifecycle); §19c (no MFA anywhere, no email verification gate before
onboarding); §19d (no rate limiting on auth, reset, invite, or MFA endpoints — login is
unthrottled password guessing).

**Why here:** depends on Phase 0's reset-flow fix being in place first (already true). Everything
else in the plan assumes a session, once compromised or superseded, actually dies — true for
the explicit revocation events above, not yet true for a session simply left idle.

**Exit criterion:** a password reset, admin-forced reset, role change, or suspension always
revokes prior sessions (met); role-tiered absolute session lifetimes match §19.2 (met) and idle
lifetimes do too (open); MFA is enforced for Super Admin and internal roles (open); auth
endpoints are rate-limited by account and IP (open).

---

### Phase 2 — Tenant isolation and the RBAC scope-spec review

**Status as of 2026-08-05: mostly done.** §2.1 (account-creation paths not granting an
Assignment), §2.2/§2.10 (LIVEY-internal roles collapsed to self-created-only on deals/tasks),
and §2.22 (Activity Timeline access, for the one shape the shipped UI actually uses — a single
deal's own timeline) are all fixed and deployed via a purpose-built `isLiveyInternalRole` /
`scopeAnyColumnEquals` mechanism in `table-policy.server.ts`, verified end-to-end with a real
governed `rm` test user in a local browser session. Three unrelated but blocking bugs were
found and fixed along the way (a broken client-side assignment-field mapping, `portal_team_members`
throwing for any LIVEY-internal caller, and a missing `is_internal` column allowlist entry) —
see `docs/implementation-status.md`'s 2026-08-01 "Phase 2" entry for the full detail.

**§2.4 update (2026-08-05):** the Deal/Customer half is now fixed too — `restricted_distributor`
gets tag-only visibility (via a new `isDistributorRole` read-scope branch reusing the
`scopeOwnerOrParticipantTag` mechanism, plus matching write-side checks in `authorizeDealActor`/
`authorizeCustomerActor`). See §2.4's own entry above for detail. Deliberately *not* extended to
Task/Ticket in the same pass — neither table has a participant-tag table to check against, so
that's tracked as a distinct, smaller remaining item, not a reason to consider this phase done.

**§2.9 correction (2026-08-05):** also already fixed, since 2026-08-02 — `applyTablePolicy`'s
`assertNoProtectedLifecycleUpdate` guard already blocks the generic table-update path from
touching `tasks.status`/`version`/`blocked_reason`/`completed_at`/`cancelled_at`, with no
super_admin bypass. See §2.9's own entry above; not something this phase still needs to do.

**Scope still open — this phase is not closed yet:** §2.4's Task/Ticket half (a Distributor
still gets flat partner-wide access to Tasks and Tickets — needs its own design decision on what
"tagged" means for those two, not a copy of the Deal/Customer mechanism), and the general case
of §2.22 (the dashboard's
broad, unfiltered activity feed — any `subject_type` other than "deal", or no `subject_id` at
all — still denies for non-super-admin; only the single-deal-timeline shape was fixed). Plus one
item new to this plan, not in the original sequencing: **DB-level enforcement.** The
2026-08-05 audit confirms all access control lives in `table-policy.server.ts` (application
layer only) — the app connects via one privileged `pg.Pool`, so any raw query that skips that
layer has no backstop, and the handful of Supabase-era RLS migrations left in
`supabase/migrations/` are dead code against the real runtime path, not a working
defense-in-depth layer. This phase should end with an explicit, written decision: either build
real per-request DB-level scoping (RLS or an equivalent), or formally retire/delete the stale
RLS migrations and document app-layer-only as the accepted model — right now it's neither,
which is its own risk (a future reader assumes RLS is live because the migrations are still
there).

**Why here:** this is the single highest-leverage phase in the whole plan — one pattern
(`getScopeSpec`/`getGenericScopeSpec` falling back to "creator/self" instead of "authorised
assignment scope") is the root cause of five of the six items above. Fixing it once, reviewed
per table, closes more distinct findings than any other phase. It must land before Phase 3,
since deal-scope correctness is a precondition for the pipeline-integrity work, and before
Phase 5, since notification-recipient correctness depends on the same scope machinery.

**Exit criterion:** every governed table's read/write scope matches the §5.5 permission matrix
for every role, not just `support_tickets`; the Task/Deal generic-write paths cannot bypass a
command-layer transition guard; every real account-creation path leaves a working `assignments`
row; the RLS question has a written answer, not an open question.

---

### Phase 3 — Deal pipeline integrity (the product's primary surface)

**Status as of 2026-08-06 (this phase's dedicated pass is now complete):** §2.7 is fixed
(`authorizeDealActor` + role-scoped `PO_REVIEW_ROLES` on `approvePO`/`requestChanges`/
`rejectOutcome`, UI narrowed to match). §2.5 is now **fully fixed** — all three collapsed-stage
paths closed: paths 1/3 (`submitDealForRegistration`/`moveDealStageForward`) on 2026-08-05, and
path 2 (`admin.deals.tsx`'s raw client write, replaced by a real `reviewDealRegistration`
command) on 2026-08-06. §2.6's outcome-review deadlock (`submitPO` locking out Won deals) was
closed 2026-08-05; its outcome-date/PO-choice/loss-reason capture was added 2026-08-06 (the
confirmed-lines/DTP/contributor-split capture remains open — see §2.6's own entry). §2.8 is now
fully fixed: the Pipeline page's `totals.pipeline` now reuses the dashboard's own canonical
DTP-based metric instead of maintaining a second, wrong one (the board-card field/label gaps —
Customer/POC/stage-specific value label/next task/participant chips/registration indicator —
remain open, see §2.8's own entry). 9e/9f/9g/9h/18c/18d — every item in this phase's original
"already-built-but-unreachable server logic" list — now have real, if in places deliberately
bounded, UI/logic: backward movement has a dialog in both `deals.tsx` and `pipeline.tsx`; line
items can be added (closing 18d outright and giving 9f's threshold/pricing engine something
real to compute from, though `createDeal` itself still captures only one free-text product+
amount pair — see 9f's own entry); the discount request/approval workflow has a minimal but
real UI in `deal-line-items.tsx` (9h); RM/ISR auto-tag on creation and PAM/KAM auto-add (with a
real, blocking Coverage Exception on missing coverage) on Testing→Qualified now exist (9g); and
18c took one bounded step — the new "Add line item" catalog picker prefers the governed
`products`/`product_variants`/`product_skus` tables when populated, falling back to
`portal_catalog_items` otherwise — without migrating any of the other five readers/writers of
the legacy table.

**Scope still open, going into the next phase:** §2.6's confirmed-final-lines/DTP/contributor-
split capture on Won (needs 9f's creation-flow reordering first); 9f's own creation-flow
reordering (line items captured *during* creation, not only as an optional post-creation add);
9g's interpretive gaps (no admin surface anywhere creates a PAM-to-Partner or KAM-to-Customer
mapping, so the Coverage Exception path is what actually runs today, and RM's "effective
regional scope" auto-tag beyond the creator's own role is unbuilt); the Coverage Exception
remediation workflow itself (Open → In Remediation → Resolved/Cancelled — only Open is ever
written); 9h's approval-snapshot requirement (approver/policy version/DTP/reason frozen at
approval time); 18c's remaining five duplicated readers of `portal_catalog_items`
(`admin.catalog.tsx`, `inventory-report.ts`, `analytics.tsx`, `export-registry.ts`, the generic
`source="catalog"` dropdown); and §2.8's board-card field gaps.

**Why here:** depends on Phase 2's scope-spec review (deal visibility must be correct before
deal *mutation* correctness matters), and is sequenced before Phase 4 because reward-award
correctness (Phase 4) is downstream of outcome-review actually being reachable (this phase).

**Exit criterion:** a deal cannot reach Won without passing outcome review (met — the deadlock is
closed); PAM/RM can approve a PO without a server-side rejection (met); pipeline value reflects
DTP on open-stage deals only, on both the dashboard and the Pipeline page (met); registration
status, pipeline stage, and outcome review never collapse into one field, in any of the three
code paths that used to do it (met); backward movement, line-item capture, and the discount
workflow are reachable through the shipped UI, not just the command layer (met, at a minimal/
bounded level per item — see each entry's own "still open" notes for exactly what's left).

---

### Phase 4 — Support and Rewards integrity

**Status as of 2026-08-05: all five live-correctness S1s are done.** §2.13 (ticket creation
crash), §2.14 (livey_support ticket-action lockout), §2.15 (redemption reservation/double-spend),
and §2.18 (reward catalog hard-delete) were fixed 2026-08-02; §2.16 (contributor-eligibility
gate) was fixed 2026-08-05 — `approvePO` now blocks Won approval outright via
`hasEligiblePartnerContributor` when no real Partner user/admin is the creator or a tagged
collaborator. See each entry above for detail.

**Scope still open:** module completeness: 13c/13d/13e/13f (ticket product/serial
model, real SLA policy, ticket notifications, human-readable ID format), 15c (reward
calculation's two-step USD→points conversion, currently collapsed into one ad hoc rate — not
re-verified against the new `reward-commands.server.ts`, worth a fresh check before assuming
still open), 15d (gadget catalogue fields: country eligibility, shipping requirement,
fulfillment task), 15e (Reward Store shows a fake points-derived tier instead of the real
governed partner tier), 15f (stat cards not clickable, all-or-nothing CSV import). Finally,
wire the already-built GyFTR client into the redemption fulfillment step (part of §17e) so an
approved redemption can actually reach `fulfilled` instead of getting stuck in `processing`
forever — worth re-checking whether the new `reward-commands.server.ts` already calls it before
assuming it doesn't.

**Why here:** independent of Phase 3, so it can run in parallel once Phase 2 lands.

**Exit criterion:** ticket creation never fails (met); `livey_support` can accept/close/wait a
ticket through the product's own UI (met); a redemption cannot be approved past the requester's
actual balance or the item's actual stock (met); a LIVEY/Distributor-created deal with no
tagged partner collaborator blocks Won approval instead of awarding the creator (met); at
least one redemption has been taken end-to-end through GyFTR to `fulfilled` (unverified either
way, re-check before assuming).

---

### Phase 5 — Notifications, News, and delivery infrastructure

**§2.12 update (2026-08-05):** already fixed 2026-08-02 — deal activity no longer writes to
`portal_news_posts` at all, see §2.12's own entry above. Removed from this phase's remaining
scope; the notification-recipient-targeting half (§2.11) is re-confirmed still open below.

**Scope:** §2.11 (notifications target the actor or the whole partner, never the record's real
assignee/watchers), 11c
(News has no lifecycle/audience/digest model at all — it's a bare photo-post CRUD), 11d (no
email/WhatsApp digest transport, no delivery-state machine, no preferences), 13e (ticket events
never notify anyone but the acting user's own browser toast), 10e's reminder/escalation half
(task reminders/SLA alerts — the workspace-view half of 10e belongs in Phase 6). This is also
where the project's **first real scheduled-job/worker infrastructure** gets built — nothing in
the codebase runs on a schedule today (confirmed: no cron, no queue library, nothing in
`package.json`), and SLA breach checks, digest sends, reminders, and draining `command_outbox`
(§17e / 18f — write-only today, nothing ever consumes it) all need one. Build the worker once
here; every later phase that needs "something happens later" (Phase 6 certificates, Phase 7
provider reconciliation) reuses it rather than inventing its own.

**Why here:** depends on Phase 2's scope-spec review (a notification recipient has to be
resolved against the same correct assignment/participant model), and the worker infrastructure
this phase builds is a hard prerequisite for Phase 6 and Phase 7's SLA/reconciliation work.

**Exit criterion:** a notification always reaches the record's actual assignee/watchers, never
the acting user or "everyone at this `partner_id`"; News has Draft/Scheduled/Published/
Expired/Archived states and real audience targeting; at least one scheduled job runs in
production and successfully drains `command_outbox`; a digest can be sent through a real email
or WhatsApp transport, not just simulated in-app.

---

### Phase 6 — Task, Insight Hub, and Auto CRM completion

**Scope:** 10c/10d (task data model — multiple related records, checklists, dependencies,
templates — and stage-triggered task generation, currently fully disconnected from the deal
pipeline), the workspace half of 10e (Today/Upcoming/Overdue/saved views), 14a/14b (a real
lesson viewer and assessment flow — right now "Continue learning" is a hardcoded-disabled
button and no learner can progress past enrollment), 14c (admin content authoring beyond
Track/Subject CRUD, audience/assignment targeting, analytics), 12a/12b (the Auto CRM Lead
entity and lifecycle — no `leads` table exists at all today — and expanding the in-app
Assistant beyond its current two intents). Note the WhatsApp *wizard* (deal drafting, ticket
browsing, OTP identity linking) is already shipped and independently audited as solid in the
2026-08-05 pass — this phase's WhatsApp-adjacent work is the *Auto CRM* layer in front of it
(lead capture, dedup, scoring, routing), not a rebuild of the wizard itself.

**Why here:** these three modules are independent of each other and of the deal-pipeline
critical path, so they can be staffed in parallel once Phase 5's scheduled-job infrastructure
exists (task reminders and Lead nurture timers both need it). Sequenced after Phase 4 because
none of them carry a live-correctness or security risk the way Phases 0-4 do — they're
capability gaps, not active bugs.

**Exit criterion:** a stage-template task generates automatically on deal-stage entry and
blocks the transition until required tasks complete; a learner can view a lesson, submit an
assessment, and receive a real Certificate record with a status lifecycle; a `leads` table
exists and at least the New→Qualified→Converted path is reachable end-to-end.

---

### Phase 7 — Remaining external integrations

**Scope:** §2.21 (Integration Operations Centre is 100% fabricated client state), 17d (Zoho
Books has no adapter at all), 17f (Zoho Sign lacks agreement revisioning, checksums, and
one-active-request enforcement), the DHL and remaining email adapters from §17e. Also the
item new to this plan: **§21's Call Center/Twilio Voice finding** — it's a real, working,
reasonably-built adapter (server-side credentials, signature-verified webhooks, idempotent
inserts, RBAC-gated) but was built outside the shared integration-record model and isn't
documented in `product.md` at all. This phase should either (a) formally add Call Center to
`product.md` and bring it up to the same outbox/reconciliation/Ops-Centre-visibility bar as
every other provider, or (b) explicitly scope it as internal tooling exempt from §17's
governance model — but not leave it undocumented and ungoverned indefinitely.

**Why here:** every item in this phase depends on Phase 5's worker infrastructure (reconciliation
jobs, retry/backoff, dead-letter handling) to be built correctly rather than as another
one-off synchronous adapter.

**Exit criterion:** the Integration Operations Centre reflects real provider state and its
operator actions (pause/resume/reconnect/disconnect) make a real backend call; Zoho Books,
DHL, and Call Center each have a documented, governed status (built-to-spec, explicitly
out-of-scope, or explicitly deferred) — none are silently absent or silently undocumented.

---

### Phase 8 — Non-functional hardening and UX polish

**Scope:** 19f (CSP/CORS/frame-ancestors headers, field-level encryption for provider secrets
and voucher codes), 19g (retention/purge jobs, data-subject access/erasure workflow), 19h
(backups, restore drills, observability/alerting), 19i (real accessibility test coverage —
axe/keyboard/screen-reader/zoom, not just "a dialog became visible"), 19j (pagination on large
lists, N+1 policy-check batching), 16b/16g (export audit trail and policy-change
versioning/dual-approval), and the remaining UX-only items that don't block any other phase:
chapter 4 (4a-4h: breadcrumb, saved views, unsaved-changes prompts, the two remaining free-text
search boxes on Rewards/Support, governed Region/State fields that still allow ad hoc value
creation), chapter 7 (7.1-7.9: per-role dashboards), chapter 8 (8.1/8.2/8.4/8.5/8.6/8.7b:
Partner detail tabs, Contact/POC entity, next-step workflow, duplicate-detection-at-creation),
and chapter 23 (23a/23b/23c: consolidating the 15-of-17 divergent reference-data dictionaries
into one sole registry per canonical term, matching §23.9's own invariant).

**Why last:** none of these block correctness or security of the core commercial flow — they're
the release-readiness bar (§19.1) and the UX completeness bar, appropriate to close out once
the product actually works end-to-end.

**Exit criterion:** the §19.1 non-functional quality bar is met; each of the 9 roles has a
distinct, populated dashboard; reference data has exactly one authoritative registry per
canonical term, with no command module silently defining its own competing constant.

---

## 21. Addendum — 2026-08-05 independent full-backend re-audit

| Attribute | Value |
| --- | --- |
| Method | 12 independent domain agents, each reading its `product.md` section(s) fresh against the current backend (`src/server`, `src/domain`, `src/integrations`, `db/schema.sql`, `supabase/migrations`) — a static spec-vs-code audit, not a line-by-line diff against this document |
| Codebase | `main`, post commit `81f48a4` ("Call Center Phase 1 — Twilio Voice calling with browser softphone") — five commits ahead of this document's 31 July baseline |
| Result | No item in §2-§19 above was found fixed since the 2026-08-01 entries already marked as such. One genuinely new item, below. |

### 21.1 New — Call Center / Twilio Voice backend (not specified anywhere in `product.md`)

**What shipped:** `src/server/twilio-voice.server.ts`, the `call_logs` table
(`db/schema.sql`, near line 1876), and `src/routes/_authenticated/calls.tsx` implement browser
softphone calling via Twilio Voice: outbound/inbound call handling, webhook-driven call-status
updates, and a calls list UI. This is real, working code, not a stub.

**Governance gap:** `product.md` has zero mentions of a call center, softphone, or Twilio Voice
anywhere in its 6,091 lines (confirmed by full-text search) — this feature was built and
shipped with no corresponding spec chapter or acceptance criteria. On its own engineering
merits it's built reasonably consistently with the rest of the codebase: credentials are
server-side only (env vars), inbound webhooks reuse the existing `verifyTwilioSignature` check
from the WhatsApp integration, and call-log inserts use `ON CONFLICT (twilio_call_sid) DO
NOTHING` for idempotency, gated by a dedicated `requireCallsCapability` RBAC check. But — like
every other integration in this codebase (see §2.21, 17e) — it does not use the shared
integration-record model (§17.2), does not write to or drain `command_outbox`/`command_inbox`,
and has no card in the Integration Operations Centre. It is not a uniquely worse offender than
the rest of §17; it is simply new, real, and currently invisible to both the spec and the
Ops Centre.

**Recommended handling:** see Phase 7 above (§20). Either add a Call Center chapter/section to
`product.md` and bring this adapter up to the same bar as the rest of §17's remediation, or
explicitly record it as an intentionally-exempt internal tool — either is acceptable, silence
is not.

### 21.2 Confirmation, not a new finding — DB-level tenant isolation

The 2026-08-05 audit independently reached the same conclusion the existing document's own
§20 (now Phase 2) sequencing note already implies: every access-control decision in this
codebase is enforced in `table-policy.server.ts` at the application layer, over a single
privileged `pg.Pool` connection with no per-request database role. The `ROW LEVEL SECURITY`
policies present in a handful of early `supabase/migrations/*.sql` files are not consulted by
the live runtime path at all. This is called out explicitly in Phase 2's scope above rather
than filed as a separate gap ID, since it's the same root cause already tracked there — but is
worth stating plainly here: there is currently no database-level backstop if an
application-layer check is ever missed on a new table or a new raw query.
